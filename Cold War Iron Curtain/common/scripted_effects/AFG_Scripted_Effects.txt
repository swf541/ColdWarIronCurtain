afgan_pakistan_peace_deal = {	#placeholder
	PAK = {
		every_owned_state = {
			limit = { is_core_of = AFG }
			transfer_state_to = AFG
		}
		every_owned_state = {
			limit = { is_core_of = KAS }
			transfer_state_to = KAS
		}
		every_owned_state = {
			limit = { is_core_of = BAN }
			transfer_state_to = BAN
		}
		every_owned_state = {
			limit = { is_core_of = SIN }
			transfer_state_to = SIN
		}
		every_owned_state = {
			limit = { is_core_of = KHA }
			transfer_state_to = KHA
		}
	}
	clear_global_flag = afghan_paki_war
}