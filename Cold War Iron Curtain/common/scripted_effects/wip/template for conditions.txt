	if = {
		limit = {
			ROOT = { tag = var:influencer1 }
			check_variable = {
				var = influencer1_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer2 }
			check_variable = {
				var = influencer2_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer3 }
			check_variable = {
				var = influencer3_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer4 }
			check_variable = {
				var = influencer4_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer5 }
			check_variable = {
				var = influencer5_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer6 }
			check_variable = {
				var = influencer6_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}
	if = {
		limit = {
			ROOT = { tag = var:influencer7 }
			check_variable = {
				var = influencer7_ammount_calc
				value = x
				compare = greater_than
			}			
		}
		break = yes
	}