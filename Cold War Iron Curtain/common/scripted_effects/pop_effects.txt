remove_pop_scale = {
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 50000
				NOT = { state_population > 100000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -5000
			
		}
		
	}
	if = {
		limit = {

			FROM.FROM = { 
				state_population > 100000
				NOT = { state_population > 1000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -10000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 1000000
				NOT = { state_population > 2000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -20000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 2000000
				NOT = { state_population > 5000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -50000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 5000000
			}		
		}
		FROM.FROM = {
			add_manpower = -500000
		}
		
	}
	if = {
		limit = {
			has_tech = atomic_weapons_armament_0
			FROM.FROM = { 
				state_population > 10000000
			}		
		}
		FROM.FROM = {
			add_manpower = -1000000
		}
		
	}
}

remove_pop_scale_two = {
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 50000
				NOT = { state_population > 100000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -1000
			
		}
		
	}
	if = {
		limit = {

			FROM.FROM = { 
				state_population > 100000
				NOT = { state_population > 1000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -2000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 1000000
				NOT = { state_population > 2000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -5000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 2000000
				NOT = { state_population > 5000000 }
			}				
		}
		FROM.FROM = {
			add_manpower = -10000
		}
		
	}
	if = {
		limit = {
			FROM.FROM = { 
				state_population > 5000000
			}		
		}
		FROM.FROM = {
			add_manpower = -25000
		}
		
	}
	if = {
		limit = {
			has_tech = atomic_weapons_armament_0
			FROM.FROM = { 
				state_population > 10000000
			}		
		}
		FROM.FROM = {
			add_manpower = -50000
		}
		
	}
}

religious_drift = {

	#Christian#
		set_variable = { ReligionChristianityCatholicTotal = 0 }
		set_variable = { ReligionChristianityProtestantTotal = 0 }
		set_variable = { ReligionChristianityOrthodoxTotal = 0 }
		set_variable = { ReligionChristianityOrientalTotal = 0 }
		#Judaic#
		set_variable = { ReligionJudaismTotal = 0 }
		#Islamic#
		set_variable = { ReligionIslamSunniTotal = 0 }
		set_variable = { ReligionIslamShiaTotal = 0 }
		set_variable = { ReligionIslamIbadiTotal = 0 }
		#Buddhism#
		set_variable = { ReligionBuddhismVajrayanaTotal = 0 }
		set_variable = { ReligionBuddhismMahayanaTotal = 0 }
		set_variable = { ReligionBuddhismTherevedaTotal = 0 }
		#Eastern#
		set_variable = { ReligionShintoTotal = 0 }
		set_variable = { ReligionTraditionalEasternTotal = 0 }
		set_variable = { ReligionConfucianTotal = 0 }
		#Indo-Iranian#
		set_variable = { ReligionHinduTotal = 0 }
		set_variable = { ReligionSikhTotal = 0 }
		set_variable = { ReligionJainTotal = 0 }
		set_variable = { ReligionTraditionalIranianTotal = 0 }
		#Irreligious#
		set_variable = { ReligionIrreligiousTotal = 0 }
		set_variable = { ReligionAtheistTotal = 0 }
		#Minor/Esoteric#
		set_variable = { ReligionSpiritualTotal = 0 }
		set_variable = { ReligionStatistTotal = 0 }
		set_variable = { ReligionCultistSafeTotal = 0 }
		set_variable = { ReligionCultistFatalTotal = 0 }
		set_variable = { ReligionFurryTotal = 0 }
		set_variable = { ReligionOtherTotal = 0 }
	
	every_state = {
		if = {
			limit = {
				OR = {
					#Christian#
					check_variable = { ReligionChristianityCatholicMission > 0 }
					check_variable = { ReligionChristianityProtestantMission > 0 }
					check_variable = { ReligionChristianityOrthodoxMission > 0 }
					check_variable = { ReligionChristianityOrientalMission > 0 }
					#Judaic#
					check_variable = { ReligionJudaismMission > 0 }
					#Islamic#
					check_variable = { ReligionIslamSunniMission > 0 }
					check_variable = { ReligionIslamShiaMission > 0 }
					check_variable = { ReligionIslamIbadiMission > 0 }
					#Buddhism#
					check_variable = { ReligionBuddhismVajrayanaMission > 0 }
					check_variable = { ReligionBuddhismMahayanaMission > 0 }
					check_variable = { ReligionBuddhismTherevedaMission > 0 }
					#Eastern#
					check_variable = { ReligionShintoMission > 0 }
					check_variable = { ReligionTraditionalEasternMission > 0 }
					check_variable = { ReligionConfucianMission > 0 }
					#Indo-Iranian#
					check_variable = { ReligionHinduMission > 0 }
					check_variable = { ReligionSikhMission > 0 }
					check_variable = { ReligionJainMission > 0 }
					check_variable = { ReligionTraditionalIranianMission > 0 }
					#Irreligious#
					check_variable = { ReligionIrreligiousMission > 0 }
					check_variable = { ReligionAtheistMission > 0 }
					#Minor/Esoteric#
					check_variable = { ReligionSpiritualMission > 0 }
					check_variable = { ReligionStatistMission > 0 }
					check_variable = { ReligionCultistSafeMission > 0 }
					check_variable = { ReligionCultistFatalMission > 0 }
					check_variable = { ReligionFurryMission > 0 }
					check_variable = { ReligionOtherMission > 0 }
				}
			}
			set_temp_variable = { driftVar = global.MissionStrength }
			multiply_temp_variable = { driftVar = state_population_k }
			#Christian#
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			#Judaic#
			add_to_variable = { ReligionJudaism = driftVar }
			#Islamic#
			add_to_variable = { ReligionIslamSunni = driftVar }
			add_to_variable = { ReligionIslamShia = driftVar }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			#Buddhism#
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			#Eastern#
			add_to_variable = { ReligionShinto = driftVar }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			add_to_variable = { ReligionConfucian = driftVar }
			#Indo-Iranian#
			add_to_variable = { ReligionHindu = driftVar }
			add_to_variable = { ReligionSikh = driftVar }
			add_to_variable = { ReligionJain = driftVar }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			#Irreligious#
			add_to_variable = { ReligionIrreligious = driftVar }
			add_to_variable = { ReligionAtheist = driftVar }
			#Minor/Esoteric#
			add_to_variable = { ReligionSpiritual = driftVar }
			add_to_variable = { ReligionStatist = driftVar }
			add_to_variable = { ReligionCultistSafe = driftVar }
			add_to_variable = { ReligionCultistFatal = driftVar }
			add_to_variable = { ReligionFurry = driftVar }
			add_to_variable = { ReligionOther = driftVar }
			
			#Christian#
			subtract_from_variable = { ReligionChristianityCatholic = 1 }
			subtract_from_variable = { ReligionChristianityProtestant = 1 }
			subtract_from_variable = { ReligionChristianityOrthodox = 1 }
			subtract_from_variable = { ReligionChristianityOriental = 1 }
			#Judaic#
			subtract_from_variable = { ReligionJudaism = 1 }
			#Islamic#
			subtract_from_variable = { ReligionIslamSunni = 1 }
			subtract_from_variable = { ReligionIslamShia = 1 }
			subtract_from_variable = { ReligionIslamIbadi = 1 }
			#Buddhism#
			subtract_from_variable = { ReligionBuddhismVajrayana = 1 }
			subtract_from_variable = { ReligionBuddhismMahayana = 1 }
			subtract_from_variable = { ReligionBuddhismThereveda = 1 }
			#Eastern#
			subtract_from_variable = { ReligionShinto = 1 }
			subtract_from_variable = { ReligionTraditionalEastern = 1 }
			subtract_from_variable = { ReligionConfucian = 1 }
			#Indo-Iranian#
			subtract_from_variable = { ReligionHindu = 1 }
			subtract_from_variable = { ReligionSikh = 1 }
			subtract_from_variable = { ReligionJain = 1 }
			subtract_from_variable = { ReligionTraditionalIranian = 1 }
			#Irreligious#
			subtract_from_variable = { ReligionIrreligious = 1 }
			subtract_from_variable = { ReligionAtheist = 1 }
			#Minor/Esoteric#
			subtract_from_variable = { ReligionSpiritual = 1 }
			subtract_from_variable = { ReligionStatist = 1 }
			subtract_from_variable = { ReligionCultistSafe = 1 }
			subtract_from_variable = { ReligionCultistFatal = 1 }
			subtract_from_variable = { ReligionFurry = 1 }
			subtract_from_variable = { ReligionOther = 1 }
		}
	}
	
	if = {
		limit = { has_idea = policy_religion_temp_0 }
		theocracy_drift = yes
	}
	else_if = {
		limit = { has_idea = policy_religion_temp_0a }
		atheist_drift = yes
	}
	else_if = {
		limit = { has_idea = policy_religion_temp_1 }
		state_religion_drift = yes
	}
	else_if = {
		limit = { has_idea = policy_religion_temp_2 }
		moralist_drift = yes
	}
	else = {
		every_controlled_state = {
			normalize_religion = yes
		}
	}
}
theocracy_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Jain }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJain = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Spiritual }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSpiritual = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Furry }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionFurry = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.TheocracyDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
state_religion_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Jain }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJain = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Spiritual }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSpiritual = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Furry }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionFurry = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.State_ReligionDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
moralist_drift = {
	if = {
		limit = { has_country_flag = PR_ChristianityCatholic }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityCatholic = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityProtestant }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityProtestant = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOrthodox }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOrthodox = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_ChristianityOriental }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionChristianityOriental = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Judaism }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJudaism = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamSunni }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamSunni = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamShia }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamShia = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_IslamIbadi }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionIslamIbadi = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismVajrayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismVajrayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismMahayana }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismMahayana = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_BuddhismThereveda }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionBuddhismThereveda = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Shinto }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionShinto = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalEastern }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalEastern = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Confucian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionConfucian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Hindu }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionHindu = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Sikh }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSikh = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Jain }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionJain = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_TraditionalIranian }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionTraditionalIranian = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Spiritual }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionSpiritual = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistSafe }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistSafe = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_CultistFatal }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionCultistFatal = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Furry }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionFurry = driftVar }
			normalize_religion = yes
		}
	}
	else_if = {
		limit = { has_country_flag = PR_Other }
		every_controlled_state = {
			set_temp_variable = { driftVar = global.MoralistDrift }
			multiply_temp_variable = { driftVar = state_population_k }
			add_to_variable = { ReligionOther = driftVar }
			normalize_religion = yes
		}
	}
}
atheist_drift = {
	
	every_controlled_state = {
		set_temp_variable = { AtheistDrift = global.AtheistDrift }
		multiply_temp_variable = { AtheistDrift = 0.75 }
		multiply_temp_variable = { AtheistDrift = state_population_k }
		add_to_variable = { ReligionAtheist = AtheistDrift }
		
		set_temp_variable = { IrreligiousDrift = global.AtheistDrift }
		multiply_temp_variable = { IrreligiousDrift = 0.25 }
		multiply_temp_variable = { IrreligiousDrift = state_population_k }
		add_to_variable = { ReligionIrreligious = IrreligiousDrift }
		
		normalize_religion = yes
	}
}