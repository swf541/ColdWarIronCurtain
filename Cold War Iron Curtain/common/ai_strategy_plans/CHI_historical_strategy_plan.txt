CHI_nationalist_historical_plan = {
	name = "Nationalist China historical plan"
	desc = "Essentially historical behavior for Nationalist China"

	allowed = {
		tag = CHI
	}
	enable = {
		original_tag = CHI
		OR = {
			AND = {
				is_historical_focus_on = yes
				#has_game_rule = {
				#	rule = CHI_ai_behavior
				#	option = DEFAULT
				#}	
			}
			has_country_flag = CHI_AI_RANDOM_HISTORICAL
			#has_game_rule = {
			#	rule = CHI_ai_behavior
			#	option = HISTORICAL
			#}
		}
	}
	abort = {
		CHI = {
			OR = {
				controls_state = 598
				controls_state = 608
				NOT = { country_exists = PRC }
				is_in_faction_with = USA 
				has_war_together_with = USA 
				has_global_flag = ROC_taiwan
			}
		}
		#has_game_rule = {
		#	rule = CHI_ai_behavior
		#	option = ALTERNATE
		#}
	}

	ai_national_focuses = {
		CHI_The_Temporary_Provisions
		CHI_Republic_of_China_Armed_Forces
		CHI_Republic_of_China_Army
		CHI_adopt_soviet_style_command_structure
		CHI_American_Arms
		CHI_amend_act_of_military_system
		CHI_The_Fall_of_the_Mainland
		CHI_The_Economy_of_Taiwan
		CHI_The_Hyperinflation_Crisis
		CHI_Reinforced_American_Ties
	
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}

}
CHI_nationalist_alternate_plan = {
	name = "Nationalist China alternate plan"
	desc = "Essentially alternate behavior for Nationalist China"

	enable = {
		original_tag = CHI
		OR = {
			controls_state = 598
			controls_state = 608
			NOT = { country_exists = PRC }
			is_in_faction_with = USA 
			has_war_together_with = USA 
			has_global_flag = chinapeace_armistice
		}
		OR = {
			AND = {
				is_historical_focus_on = no
				#has_game_rule = {
				#	rule = CHI_ai_behavior
				#	option = DEFAULT
				#}	
			}
			has_country_flag = CHI_AI_RANDOM_HISTORICAL
			#has_game_rule = {
			#	rule = CHI_ai_behavior
			#	option = ALTERNATE
			#}
		}
	}
	abort = {
		OR = {
			PRC = {
				OR = {
					controls_state = 601
					controls_state = 592
					has_global_flag = ROC_taiwan
				}
			}
			#has_game_rule = {
			#	rule = CHI_ai_behavior
			#	option = HISTORICAL
			#}
		}
	}

	ai_national_focuses = {
		
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}

}