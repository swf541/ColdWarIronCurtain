scripted_diplomatic_actions = {
	opt_embargo_action  = {
		allowed = {}
		visible = { 
			check_variable = {
				ROOT.trade_embargo_check^THIS = 0
			}
		}
		selectable = {
			NOT = { has_war_with = ROOT }
			custom_trigger_tooltip = {
				tooltip = Embargo_Action_1_TT
				check_variable = {
					ROOT.trade_embargo_check^THIS = 0
				}
			}
		}
		requires_acceptance = no
		cost = 50
		command_power = 0
		show_acceptance_on_action_button = no
		icon = 1
		on_sent_effect = {}
		# runs when the action is properly sent and accepted
		complete_effect = {
			set_variable = {
				ROOT.trade_embargo_check^THIS = 1
			}
		}
		reject_effect = {} # runs when the action is properly sent and rejected
		can_be_sent = {} # send button will be disabled if no
		send_description = embargo_target_send # if there is no scripted gui, this desc will be used in send yes/no pop up
		reset_receive_effect = {}
		can_be_accepted = { always = no } # accept button will be disabled if no
		receive_description = embargo_target_receive # if there is no scripted gui, this desc will be used in send yes/no pop up
		# ai_will_do to decide if AI should send this action or not
		ai_desire = {
			base = 0
		}
	}
	opt_lift_embargo_action = {
		allowed = {}
		visible = { 
			check_variable = {
				ROOT.trade_embargo_check^THIS = 1
			}
		}
		selectable = {
			NOT = { has_war_with = ROOT }
			custom_trigger_tooltip = {
				tooltip = Embargo_Action_2_TT
				check_variable = {
					ROOT.trade_embargo_check^THIS = 1
				}
			}
		}
		requires_acceptance = no
		cost = 50
		command_power = 0
		show_acceptance_on_action_button = no
		icon = 1
		on_sent_effect = {}
		# runs when the action is properly sent and accepted
		complete_effect = {
			set_variable = {
				ROOT.trade_embargo_check^THIS = 0
			}
		}
		reject_effect = {} # runs when the action is properly sent and rejected
		can_be_sent = {} # send button will be disabled if no
		send_description = embargo_lifted_target_send # if there is no scripted gui, this desc will be used in send yes/no pop up
		reset_receive_effect = {}
		can_be_accepted = { always = no } # accept button will be disabled if no
		receive_description = embargo_lifted_target_receive # if there is no scripted gui, this desc will be used in send yes/no pop up
		# ai_will_do to decide if AI should send this action or not
		ai_desire = {
			base = 0
		}
	}
}
