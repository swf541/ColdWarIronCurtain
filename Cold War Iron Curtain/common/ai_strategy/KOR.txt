USA_support_KOR = {
	
	enable = {		
		tag = USA
	}
	
	ai_strategy = {
		type = send_volunteers_desire
		id = "KOR"			
		value = 500
	}

}

UN_support_KOR = {
	
	enable = {	
	    OR = {
			tag = AST #does
			tag = CAN #doesn't
			tag = SAF #sends 
			tag = COL #does
			tag = SIA #sends
			tag = ETH #sends 
			tag = JAP #doesn't
			tag = CHI #sends
			tag = FRA #sends
			tag = ENG #sends
			tag = BEL #doesn't
			tag = HOL #doesn't
			tag = LUX #does
			tag = GRE #doesn't
			tag = TUR #doesn't
			tag = SPR #does
			tag = NZL
			tag = "PHI"
		}
		has_global_flag = korean_war
	}
    abort = {
		NOT = { has_global_flag = korean_war }
	}
	
	ai_strategy = {
		type = send_volunteers_desire		
		id = "KOR"			
		value = 5000
	}
	ai_strategy = {
		type = support
		id = "KOR"			
		value = 5000
	}
	ai_strategy = {
		type = befriend
		id = "KOR"			
		value = 5000
	}
}