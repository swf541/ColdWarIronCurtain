#British buffer unit in West Germany
ENG_unit_buffer_for_europe = {
	allowed = {
		tag = ENG
	}
	
	abort_when_not_enabled = yes
	
	ai_strategy = {
		type = put_unit_buffers
		
		order_id = 2
		
		# West Germany, North
		states = { 
			56
		}
		
		ratio = 0.1
		area = europe
		
		subtract_invasions_from_need = no
	}
}	
ENG_build_peacetime_army = {
	allowed = { tag = ENG }
	enable = { 
		date > 1949.1.1
		has_war = no
		OR = { 	has_army_manpower = { size < 250000 }
				strength_ratio = { tag = SOV ratio < 0.1 } 
				strength_ratio = { tag = PRC ratio < 0.05 }
				strength_ratio = { tag = USA ratio < 0.3 }
				strength_ratio = { tag = FRA ratio < 0.75 }
				strength_ratio = { tag = WGR ratio < 1.2 }    
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}
ENG_build_wartime_army = {
	allowed = { tag = ENG }
	enable = { 
		date > 1949.1.1
		has_war = yes
		has_army_manpower = { size < 2000000 }
	}
	abort_when_not_enabled = yes

	ai_strategy = { type = build_army id = motorized value = 500 }
	ai_strategy = { type = build_army id = infantry value = 400 }
	ai_strategy = { type = force_build_armies value = 300 }
}