CUB_support_ETH = {
	allowed = {
		tag = CUB
		OR = {
			tag = DRY
		}
	}		
	enable = {		
		tag = CUB
		OR = {
			tag = DRY
		}
	}
	
	abort_when_not_enabled = yes	

	ai_strategy = {
		type = send_volunteers_desire
		id = "ETH"			
		value = 500
	}

}

ETH_ogaden_war_support_supplies = {
	allowed = {
		tag = CUB
		OR = {
			tag = DRY
			tag = SOV
			tag = ISR
			tag = POL
			tag = DDR
			tag = KPA
		}
	}		
	
	abort_when_not_enabled = yes	

	enable = {		
		has_global_flag = ogaden_war
		tag = CUB
		OR = {
			tag = DRY
			tag = SOV
			tag = ISR
			tag = POL
			tag = DDR
			tag = KPA
		}
	}
	
	ai_strategy = {
		type = send_lend_lease_desire
		id = "ETH"			
		value = 500
	}
}