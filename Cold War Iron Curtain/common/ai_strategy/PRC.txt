# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence
# alliance
# ignore
# invade

PRC_supports_north = {
	enable = {
		tag = PRC
		KPA = { has_war_with = KOR }
	}
	abort = {
		NOT = { country_exists = KPA }
	}
	 ai_strategy = {
		type = area_priority
		id = korea
		value = 5000
	}  
	ai_strategy = {
		type = conquer
		id = "KOR"
		value = 500
	}		
	ai_strategy = {
		type = invade
		id = "KOR"
		value = 500
	}		
	
	ai_strategy = {
		type = befriend
		id = "KPA"
		value = 200
	}	

	ai_strategy = {
		type = protect
		id = "KPA"
		value = 100
	}	
	ai_strategy = {
		type = front_unit_request
		#you can define a target in following ways. you can define more than one

		tag = KOR 				# target against a specific country
		country_trigger = { always = no } 	# a trigger to check agaisnt a specific country. scope is enemy country from scope is our country
		state_trigger = { always = no } 	# a trigger to check agaisnt a state. scope is state. from scope is enemy country from.from scope is our country


		value = 120 # default 0, higher prio strats will override lower
	}
}