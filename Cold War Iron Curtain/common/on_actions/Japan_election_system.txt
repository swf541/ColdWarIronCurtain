on_actions = {
	on_monthly_JAP = {
		effect = {
			JAP_1st_every_monthly_refresh = yes
			add_to_variable = { pulse_month = 1 }
			if = {
				limit = { check_variable = { pulse_month = 12 } }
				set_variable = { pulse_month = 0 }
			}
		}
	}
}
