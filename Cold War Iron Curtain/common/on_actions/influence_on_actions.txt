on_actions = {

    on_daily = {
        effect = {
			# daily topbar refresh
			if = { limit = { is_ai = no }
				set_temp_variable = { show = 1 }
				subtract_from_temp_variable = { show = refresh_topbar }
				set_variable = { refresh_topbar = show }
			}
			# The old daily influencing tick was retired by the sphere rework (2026-07-19).
			# Influence now accrues monthly from priorities — see CWIC_sphere_on_actions.txt.
        }
    }
}
