scripted_gui = {
	WMD_tab_menu = {

		context_type = player_context

		window_name = "WMD_tab"
		visible = {
			check_variable = { show_wmd = 1 }
		}	
	}
	wmd_topbar_button = {

		context_type = player_context

		window_name = "wmd_topbar_button"
		parent_window_name = country_menu_window
		effects = {
			wmd_tab_open_click = {
				ROOT = {
					set_temp_variable = { show = 1 }
					subtract_from_temp_variable = { show = show_wmd }
					set_variable = { show_wmd = show }
				}
			}	
		}
	}
	wmd_nuclear_powers_list = {
        context_type = player_context
        window_name = "wmd_list"
		parent_window_name = WMD_tab_menu
        dynamic_lists = {
            nuclear_power_listing = {
                array = root.nuclear_power_array
                change_scope = no
            }
        }
    }
}