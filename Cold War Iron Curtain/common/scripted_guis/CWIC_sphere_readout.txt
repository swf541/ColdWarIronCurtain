# ============================================================
# CWIC Sphere of Influence — small always-on readout in the diplomacy view.
# Just a text line (no buttons); the actions are native diplomatic actions.
# Scope = selected country (target); ROOT = viewing player.
# ============================================================
scripted_gui = {
	CWIC_sphere_readout = {
		context_type = selected_country_context
		parent_window_token = selected_country_view_diplomacy
		window_name = "CWIC_sphere_readout"
		visible = {
			ROOT = { is_sphere_capable = yes }
			NOT = { is_sphere_capable = yes }
			NOT = { tag = ROOT }
		}
		effects = {}
	}
}
