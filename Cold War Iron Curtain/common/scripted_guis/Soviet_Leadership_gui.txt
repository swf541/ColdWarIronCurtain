scripted_gui = {
	soviet_leadership_menu = {
		context_type = player_context
		 
		window_name = "Soviet_Leadership_Window"
        #dirty = comecon_redux_variable

		visible = {
            tag = SOV 
			check_variable = { leadership_display > 0 }
		}

		triggers = {
            
        }
    }
}