scripted_gui = {


	UN_tab_menu = {

		context_type = player_context

		window_name = "UN_tab"
		visible = {
			check_variable = { show_un = 1 }
            #always = yes
		}	
		effects = {
			exitbutton_un_click = {
				ROOT = {
					set_temp_variable = { show = 1 }
					subtract_from_temp_variable = { show = show_un }
					set_variable = { show_un = show }
				}			
			}
		}
    }

	country_un_action_list = {
        context_type = player_context
        window_name = "un_action_target_menu"
		parent_window_name = UN_tab
        dynamic_lists = {
            un_action_listing_grid = {
                array = ROOT.un_option_list
                change_scope = yes
				entry_container = un_action_target_entry
            }
        }
    }
}