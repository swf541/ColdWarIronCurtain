	#Generic Bonuses
	Military_Research_Grant = { #                      2x 15% General Military Research Boosts
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			OR = { 
				has_government = conservative
				has_country_flag = military_research_grantEX
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing
			add_tech_bonus = {
				name = generic_mil_bonus
				bonus = 0.15
				uses = 2
				category = armor
				category = artillery
				category = infantry_weapons
				category = naval_equipment
				category = land_doctrine
				category = air_doctrine
				category = naval_doctrine
				category = infantry_tech
				category = air_equipment
				category = support_tech
				category = helicopter
				category = missle
			}
			
			
            effect_tooltip = {
			    add_political_power = 30

		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_tech_bonus = {
						name = generic_mil_bonus
						bonus = 0.15
						uses = 2
						category = armor
						category = artillery
						category = infantry_weapons
						category = naval_equipment
						category = land_doctrine
						category = air_doctrine
						category = naval_doctrine
						category = infantry_tech
						category = air_equipment
						category = support_tech
						category = helicopter
						category = missle
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Encourage_Family_Unity = { #                      +2.5% Stability  Liberal Conservativism + Conservative
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			OR = { 
				has_government = conservative
				has_government = Liberal_Conservatism
				has_government = Christian_Democratic
				has_government = neutrality
				has_government = fascism
				has_government = nationalism
				has_government = radical_shiaism
				has_government = islamism
				has_government = Salafism
				has_country_flag = Encourage_Family_UnityEX
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing
			add_stability = 0.025
			
            effect_tooltip = {
			    add_political_power = 30

		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_stability = 0.025
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	#Timed Idea Bonuses
	Environmental Protectionism = {	#have_furries_control_government
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = have_furries_control_government }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = have_furries_control_government
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = have_furries_control_government
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Civilian Industry Zoning = {			#{180 days} civilian_boost_general
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = civilian_boost_general }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_timed_idea = {
					idea = civilian_boost_general
					days = 180
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_timed_idea = {
						idea = civilian_boost_general
						days = 180
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Infrastructure Project = {				#{180 days} infrastructure_boost_general
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = infrastructure_boost_general }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_timed_idea = {
					idea = infrastructure_boost_general
					days = 180
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_timed_idea = {
						idea = infrastructure_boost_general
						days = 180
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Military_Industrial_Buildup = { #                      {180 days} military_boost_general
       allowed = { has_country_flag = parliament_generic_enabled }
		
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = military_boost_general }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_timed_idea = {
					idea = military_boost_general
					days = 180
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_timed_idea = {
						idea = military_boost_general
						days = 180
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Arms_Buildup = { #                      {180 days} arms_boost_general
		
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = arms_boost_general }
			OR = { 
				NOT = { has_government = Social_Democratic }
				has_country_flag = Arms_BuildupEX
			}
			OR = { 
				NOT = { has_government = democratic }
				has_country_flag = Arms_BuildupEX
			}
			OR = { 
				NOT = { has_government = Social_Liberalism }
				has_country_flag = Arms_BuildupEX
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_timed_idea = {
					idea = arms_boost_general
					days = 180
				}
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_timed_idea = {
						idea = arms_boost_general
						days = 180
					}
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	#Healthcare Policies
	
	Advanced_Universal_Health_Care = {				#communism_healthcare
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = communism_healthcare }
			OR = { 
				has_government = fascism
				has_government = communism
				has_government = trotskyism
				has_government = maoism
				has_country_flag = Advanced_Universal_Health_CareXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = communism_healthcare
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = communism_healthcare
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Universal_Healthcare = {						#socialism_healthcare
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = socialism_healthcare }
			NOT = { has_idea = communism_healthcare }
			OR = { 
				AND = {
					OR = { 
						has_government = fascism
						has_government = communism
						has_government = trotskyism
						has_government = maoism
					}
					NOT = { has_idea = communism_healthcare }
				}
				has_government = socialist
				has_government = Social_Democratic
				has_government = democratic
				has_government = Social_Liberalism
				has_country_flag = Universal_HealthcareXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = socialism_healthcare
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = socialism_healthcare
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		

	
	Medical_Science_Funding = {						#medical_science
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = medical_science }
			OR = { 
				AND = {
					OR = { 
						has_government = Liberal_Conservatism
						has_government = centrist
					}
					NOT = { has_idea = medical_apotheosis }
				}
				has_government = nationalism
				has_country_flag = Medical_Science_FundingXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = medical_science
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = medical_science
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	
	Expand_Funding_for_Medical_Laboratories = {		#medical_apotheosis
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = medical_apotheosis }
			OR = { 
				has_government = Liberal_Conservatism
				has_government = centrist
				has_country_flag = Medical_ApotheosisXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = medical_apotheosis
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = medical_apotheosis
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	
	Reduce_Government_Medical_Cost = { #                      reduce_medical_bills
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = reduce_medical_bills }
			OR = { 
				AND = {
					OR = {
						has_government = liberal
						has_government = neutrality
					}
					NOT = { has_idea = Privatize_Healthcare }
				}
				has_government = conservative
				has_government = monarchism
				has_country_flag = Reduce_Government_Medical_CostXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = reduce_medical_bills
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = reduce_medical_bills
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	
	Privatize_Healthcare = {		#privatized_healthcare
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = privatized_healthcare }
			OR = { 
				has_government = liberal
				has_government = neutrality
				has_country_flag = Privatize_HealthcareXP	
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = privatized_healthcare
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = privatized_healthcare
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	#Government Spending
	Welfare_Statism = {						#welfare_state_plus
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = welfare_state_plus }
			OR = { 
				has_government = communism
				has_government = trotskyism
				has_government = maoism
				has_government = socialist
				has_country_flag = Welfare_StatismXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = welfare_state_plus
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = welfare_state_plus
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	
	Enact_Social_Safety_Net = {				#safety_net
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = safety_net }
			OR = { 
				AND = {
					OR = {
						has_government = communism
						has_government = trotskyism
						has_government = maoism
						has_government = socialist					
					}
					NOT = { has_idea = Welfare_Statism }
				}
				has_government = Social_Democratic
				has_government = Social_Liberalism
				has_government = democratic
				has_country_flag = Enact_Social_Safety_NetXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = safety_net
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = safety_net
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	Economic_Subsidies = {				#subsidies_basic
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = subsidies_basic }
			OR = { 
				AND = {
					has_government = centrist
					NOT = { has_idea = subsidy_economy }
				}
				has_government = nationalism
				has_government = monarchism
				has_government = Liberal_Conservatism
				has_country_flag = Economic_SubsidiesXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = subsidies_basic
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = subsidies_basic
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
	Mass_Subsidization = {		#subsidy_economy
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = subsidy_economy }
			OR = { 
				has_government = centrist
				has_country_flag = Mass_SubsidizationXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = subsidy_economy
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = subsidy_economy
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Reduce_Government_Spending = { #                      save_money
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = save_money }
			OR = { 
				AND = {
					OR = {
						has_government = liberal
						has_government = fascism
						has_government = neutrality
					}
					NOT = { has_idea = Welfare_Statism }
				}
				has_government = conservative
				has_government = Liberal_Conservatism
				has_country_flag = Reduce_Government_Spending_NetXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = save_money
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = save_money
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			

	Centralize_Corporate_Control = {	#save_more_money
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = save_more_money }
			OR = { 
				has_government = liberal
				has_government = fascism
				has_government = neutrality
				has_country_flag = Centralize_Corporate_ControlXP
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = save_more_money
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = save_more_money
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	
	#Economic Policies
	Seize_the_Means_of_Production
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = communism_intensifies }
			OR = { 
				has_government = socialist
				has_government = communism
				has_government = maoism
				has_government = trotskyism
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = communism_intensifies
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = communism_intensifies
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }				
	Encourage_Collectivization
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = activate_communism }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = activate_communism
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = activate_communism
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			

	Rural_Development = {
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = rural_development_spirit }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = rural_development_spirit
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = rural_development_spirit
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	
	Embrace_Capitlalism = { #                      activate_capitalism
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = activate_capitalism }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = activate_capitalism
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = activate_capitalism
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			

	Tax_Reforms = { #                      capitalism_intensifies
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = capitalism_intensifies }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = capitalism_intensifies
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = capitalism_intensifies
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	#Military Spending Policies
	Vastly_Reduce_Military_Spending = {
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = cheap_ass_army }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = cheap_ass_army
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = cheap_ass_army
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }				
	
	Reduce Military Spending = {
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = cheap_army }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = cheap_army
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = cheap_army
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }				
	
	Increase_Military_Spending = { #                      more_mil_staff
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = more_mil_staff }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = more_mil_staff
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = more_mil_staff
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }				
	Largely_Increase_Military_Spending = { #                      even_more_mil_staff
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = more_mil_staff }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = more_mil_staff
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = more_mil_staff
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	#Veterans Affairs
	Veteran_Affairs_and_War_Relief = {		#vet_aff_and_relief
       available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = vet_aff_and_relief }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = vet_aff_and_relief
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = vet_aff_and_relief
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	
	Veteran_Affairs = {						#veteran_affairs_generic
       available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = veteran_affairs_generic }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = veteran_affairs_generic
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = veteran_affairs_generic
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	
	Take_Conservative_Labor_Approach = { #                      gen_balanced_workers_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_balanced_workers_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_balanced_workers_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_balanced_workers_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	
	Take_Balanced_Labor_Approach = {		#gen_balanced_workers_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_balanced_workers_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_balanced_workers_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_balanced_workers_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Strengthen_the_Workers = {				#gen_pro_union_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_union_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_union_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_union_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Workers_Bill_of_Rights = {				#gen_pro_union_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_union_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_union_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_union_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Restrict_Unions = {						#gen_pro_employer_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_employer_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_employer_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_employer_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Mass_Deregulation = {					#gen_pro_employer_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_employer_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_employer_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_employer_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }
	
	Freedom_of_Consumption = { #                     gen_materialist_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_materialist_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_materialist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_materialist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	Embrace_Technological_Progress = { #             gen_materialist_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_materialist_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_materialist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_materialist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	A_Green_Economy = { #                      		 gen_environmentalist_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_environmentalist_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_environmentalist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_environmentalist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	Embrace_Environmentalism = { #                   gen_environmentalist_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_environmentalist_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_environmentalist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_environmentalist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	Pragmatic_Environmental_Outlook = { #            gen_balanced_outlook_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_balanced_outlook_2 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_balanced_outlook_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_balanced_outlook_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	
	Conservative_Environmental_Outlook = { #                      gen_balanced_outlook_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_balanced_outlook_1 }
			OR = { 
				has_government = conservative
				
				
				
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_balanced_outlook_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_balanced_outlook_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	#LGBT Policies
	Death_Penalty_for_Homoxeuality = {		#gen_notsaga_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_notsaga_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_notsaga_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_notsaga_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	Criminalization_of_Homosexuality = {		#gen_notsaga_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_notsaga_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_notsaga_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_notsaga_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Legalize_Homosexuality = {		#gen_semighey_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_semighey_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_semighey_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_semighey_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	LGBT_Legal_Protections = {		#gen_semighey_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_semighey_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_semighey_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_semighey_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Pro_SAGA_Curriculum = {		#gen_ghey_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_ghey_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_ghey_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_ghey_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Allow_LGBT_Service_Members = {		#gen_ghey_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_ghey_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_ghey_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_ghey_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	#Social Policies
	Social_Liberal_Social_Outlook = { #            gen_equal_1    
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_equal_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_equal_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_equal_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	
	Intersectional_Social_Outlook = { #                      gen_equal_2
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_equal_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_equal_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_equal_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Reactionary_Social_Outlook = { #            gen_archy_1    
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_archy_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_archy_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_archy_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	
	
	Fascist_Stalwartism = { #                      gen_archy_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_archy_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_archy_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_archy_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Traditional_Social_Outlook = { #                      gen_trad_1
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_trad_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_trad_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_trad_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Conservative_Social_Outlook = { #                      gen_trad_2
        available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_trad_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_trad_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_trad_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	
	#Troops Policy
	
	Military_Industrial_Redistribution = { #gen_pacifist_1
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pacifist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pacifist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pacifist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Embrace_Pacifism = { #gen_pacifist_2
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pacifist_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pacifist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pacifist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Revanchist_Rhetoric = { #gen_jingoist_1
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_jingoist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_jingoist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_jingoist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	
	Spread_the_Revolution = { #gen_jingoist_2
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_jingoist_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_jingoist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_jingoist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Support_Our_Boys = { #gen_pro_mil_2
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_mil_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_mil_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_mil_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Increase_the_Military_Budget = { #                      gen_pro_mil_1
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_pro_mil_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_pro_mil_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_pro_mil_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	

	#Trade Policies
	Conservative_Economic_Approach = { #                      gen_openist_1
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_openist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_openist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_openist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		
	International_Communism = { #                      gen_openist_1
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_openist_1 }
			OR = { 
				has_government = communism
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_openist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_openist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }		

	Centrist_Economic_Approach = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_openist_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_openist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_openist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	The_Foreign_Mission = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_globalist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_globalist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_globalist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	Global_Communism = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_globalist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_globalist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_globalist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Embrace_Globalism = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_globalist_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_globalist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_globalist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Communism_in_One_Country = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_nationalist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_nationalist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_nationalist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Mercantilism = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_nationalist_1 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_nationalist_1
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_nationalist_1
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	
	Economic_Isolationism = {
	    available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = gen_nationalist_2 }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = gen_nationalist_2
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = gen_nationalist_2
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	
	#Education
	Creative_Education_Reforms = {		#creative_education
		available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = creative_education }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = creative_education
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = creative_education
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	Mass_Academic_Funding = {			#academic_state
		available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = academic_state }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = academic_state
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = academic_state
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	Increase_Education_Spending = {		#more_escuelas
		available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = more_escuelas }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = more_escuelas
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = more_escuelas
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
	Prayer_in_Schools = {				#patriotic_schools
		available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = patriotic_schools }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = patriotic_schools
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = patriotic_schools
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }			
	Military_Colleges = {				#military_colleges	
 		available = {
            NOT = { has_country_flag = law_passing }
			NOT = { has_idea = military_colleges }
			OR = { 
				has_government = conservative
			}
        }
        visible = {
            NOT = { has_country_flag = law_passing }
        }
		cost = 100
        days_re_enable = 1250
		
		complete_effect = {
	        set_country_flag = law_passing			
            effect_tooltip = {
				add_ideas = military_colleges
		    } 			
        }
		days_remove = 50
		remove_effect = {
			hidden_effect = {
			    if = {
					limit = { 
						check_variable = { supseat > majorityThreshold } 
					}
					add_ideas = military_colleges
				}
				if = {
				    limit = { 
						check_variable = { supseat < majorityThreshold } 
					}
				}
				set_variable = { supseat = 0 }
				
				clr_country_flag = law_passing
            }			
        }
    }	