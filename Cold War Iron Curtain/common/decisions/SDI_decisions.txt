SDI_category = {
	SDI_increase_funding = {
		icon = generic_construction

		visible = {
			has_completed_focus = USA80MIL_Secure_Additional_Funding
		}

		available = {
		}

		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 200 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money200

		days_re_enable = 120

		complete_effect = {
			add_to_variable = {
				currentMoney = -200
			}
			add_to_variable = {
				sdiFunds = 200
			}
			custom_effect_text = sdi_increase_funding_effect_tt
		}

		ai_will_do = {
			factor = 0
			modifier = {
				if = {
					check_variable = { var = currentMoney value = 200 compare = greater_than_or_equals }
				}
				add = 10
			}
		}
	}
	SDI_recruit_scientists = {
		icon = generic_construction

		visible = {
			has_completed_focus = USA80MIL_Recruit_Additional_Scientists_and_Contractors
		}

		available = {
		}

		custom_cost_trigger = {
			check_variable = { var = sdiFunds value = 30 compare = greater_than_or_euals }
		}
		custom_cost_text = sdiFunds_cost_30														
		fire_only_once = yes
		
		complete_effect = {
			add_to_variable = {
				sdiFunds = -30
			}
			add_to_variable = {
				sdiProgress = 3
			}
			custom_effect_text = SDI_recruit_scientists_effect_tt
		}
	}
}