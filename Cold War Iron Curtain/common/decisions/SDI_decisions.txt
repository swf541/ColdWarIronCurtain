SDI_category = {
	SDI_increase_funding = {
		icon = generic_construction

		visible = {
			has_completed_focus = USA80MIL_Secure_Additional_Funding
		}

		available = {
		}

		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 200 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money200
		
		days_re_enable = 120

		complete_effect = {
			add_to_variable = {
				currentMoney = -200
			}
			add_to_variable = {
				sdiFunds = 200
			}
		}

		ai_will_do = {
			factor = 0
			modifier = {
				if = {
					check_var = { var = currentMoney value = 200 compare = greater_than_or_equals }
				}
				add = 10
			}
		}
	}
}