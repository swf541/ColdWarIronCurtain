economy_decisions_reenable = {
	unhide_economy_decisions = {
	
		visible = { has_country_flag = hide_economic_decisions }
		complete_effect = { clr_country_flag = hide_economic_decisions }
	}
}
economy_decisions = {
	hide_economy_decisions = {
	
	icon = hide
	
		visible = { NOT = { has_country_flag = hide_economic_decisions } }
		complete_effect = { set_country_flag = hide_economic_decisions }
	}
	SOV_Reperation_Payments = {
	
		icon = paid

		allowed = {
			tag = HUN
		}
		
		available = {
			num_of_civilian_factories_available_for_projects > 4
		}
		visible = {
			has_country_flag = Loan_Repayment_Enabled
			OR = {
				has_idea = soviet_reparations
				has_idea = soviet_reparations2
				has_idea = soviet_reparations3
				has_idea = soviet_reparations4
			}

		}
		
		modifier = {
			civilian_factory_use = 5
		}
		
		ai_will_do = {
			factor = 1
		}
		days_remove = 180
		remove_effect = {
			if = {
				limit = {
					has_idea = soviet_reparations
				}
				remove_ideas = soviet_reparations
				add_ideas = soviet_reparations2
			}
			else_if = {
				limit = {
					has_idea = soviet_reparations2
				}
				remove_ideas = soviet_reparations2
				add_ideas = soviet_reparations3
			}
			else_if = {
				limit = {
					has_idea = soviet_reparations3
				}
				remove_ideas = soviet_reparations3
				add_ideas = soviet_reparations4
			}
			else = {
				remove_ideas = soviet_reparations4
			}
		}
		
	}

####Pan European Union Membership Application###

	PAEU.1 = {
		
		visible = {
			capital_scope = { is_on_continent = europe }

			NOT = {
				has_global_flag = European_Waiting
			}
			NOT = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
			SOV = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
		}

		cost = 150

		complete_effect = {
			every_country = {
				limit = {
					has_dynamic_modifier = {
						modifier = federation_european_union
					}
				}
				country_event = SOV_Beria_Struggle.37
			}
			set_global_flag = { flag = European_Waiting value = 1 days = 30 }
			set_country_flag = PAEU_2_Follow_Up

			custom_effect_tooltip = EU_Waiting_TT
		}
	}
	PAEU.2 = {
		
		visible = {
			capital_scope = { is_on_continent = europe }
			NOT = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
			SOV = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}

			has_country_flag = PAEU_2_Follow_Up

			NOT = {
				has_global_flag = European_Waiting
			}
			check_variable = {
				global.federation_european_union_entrance > 1
			}
		}

		cost = 150

		complete_effect = {
			every_country = {
				limit = {
					has_dynamic_modifier = {
						modifier = federation_european_union
					}
				}
				country_event = SOV_Beria_Struggle.35
			}
			add_dynamic_modifier = {
				modifier = federation_european_union
			}

			set_variable = { global.federation_european_union_entrance = 0 }
		}
	}
	PAEU.3 = {

		visible = {
			has_dynamic_modifier = {
				modifier = federation_european_union
			}
		}

		cost = 300

		complete_effect = {
			remove_dynamic_modifier = {
				modifier = federation_european_union 
			}
		}
	}	
	#The TAZARA Railway (PRC Decision)
	
 	Request_Chinese_Technical_Advisors = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			check_variable = { grossdomesticproduct > 35 }
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_technical_advisors
				days = 3650
			}
			set_variable = { THIS.idea_len_@token:PRC_technical_advisors = 3650 }
			set_variable = { THIS.idea_date_@token:PRC_technical_advisors = global.num_days }
			set_temp_variable = { percent_change = 7 }
			set_temp_variable = { tag_index = PRC.id }
			set_temp_variable = { influence_target = THIS.id }
			change_influence_percentage = yes 
			clr_country_flag = found
			recalculate_influence = yes	


		}
	}


 	Request_Chinese_Educational_Support = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_idea = policy_education_temp_0
				has_idea = policy_education_temp_1
			}
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_Educational_Advisors
				days = 3650
			}
			set_variable = { THIS.idea_len_@token:PRC_Educational_Advisors = 3650 }
			set_variable = { THIS.idea_date_@token:PRC_Educational_Advisors = global.num_days }
			set_temp_variable = { percent_change = 7 }
			set_temp_variable = { tag_index = PRC.id }
			set_temp_variable = { influence_target = THIS.id }

		}
	}


 	Request_Chinese_Medical_Assistance = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_idea = policy_healthcare_temp_0
				has_idea = policy_healthcare_temp_1
			}
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_Health_Advisors
				days = 3650
			}
			set_variable = { THIS.idea_len_@token:PRC_Health_Advisors = 3650 }
			set_variable = { THIS.idea_date_@token:PRC_Health_Advisors = global.num_days }
			set_temp_variable = { percent_change = 7 }
			set_temp_variable = { tag_index = PRC.id }
			set_temp_variable = { influence_target = THIS.id }

		}
	}



 	Request_Chinese_Military_Advisors = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = chinese_advisors
				days = 3650
			}
			set_variable = { THIS.idea_len_@token:chinese_advisors = 3650 }
			set_variable = { THIS.idea_date_@token:chinese_advisors = global.num_days }
			set_temp_variable = { percent_change = 7 }
			set_temp_variable = { tag_index = PRC.id }
			set_temp_variable = { influence_target = THIS.id }
		}
	}

	Seek_Chinese_Financial_Aid = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 1095


		cost = 0

		complete_effect = {
			set_temp_variable = { percent_change = 7 }
			set_temp_variable = { tag_index = PRC.id }
			set_temp_variable = { influence_target = THIS.id }

			add_to_variable = { 
				currentMoney = 25 
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	Pay_Off_Debt = {

		icon = paid

		available = {
			check_variable = { currentMoney > currentDebt }
			check_variable = { currentDebt > 0 }
			NOT = {
				has_country_leader = {
					name = "Lazar Kaganovich"
					ruling_only = yes
				}
			}
		}

		fire_only_once = no


		cost = 0

		complete_effect = {
			subtract_from_variable = { currentMoney = currentDebt }
			for_each_loop = {
				array = debtPaymentArray
				index = i
				
				set_variable = { debtTotalsArray^i = 0 } 
				set_variable = { debtPaymentArray^i = 0 } 	
				set_variable = { debtOwnerArray^i = 0 }
			}
			set_variable = { currentDebt = 0 }
			set_variable = { debtPayment = 0 }
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = {
							influence_array^v = GER
						}
					}
					set_temp_variable = { percent_change = influence_array_val^v }
					multiply_temp_variable = { percent_change = -1 }
					set_country_flag = found
				}
			}
			set_temp_variable = { tag_index = GER.id }
			set_temp_variable = { influence_target = THIS.id }
			change_influence_percentage = yes
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	
	
}