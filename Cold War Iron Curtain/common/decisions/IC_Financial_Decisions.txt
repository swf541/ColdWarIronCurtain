economy_decisions_reenable = {
	unhide_economy_decisions = {
	
		visible = { has_country_flag = hide_economic_decisions }
		complete_effect = { clr_country_flag = hide_economic_decisions }
	}
}
economy_decisions = {
	hide_economy_decisions = {
	
	icon = hide
	
		visible = { NOT = { has_country_flag = hide_economic_decisions } }
		complete_effect = { set_country_flag = hide_economic_decisions }
	}
	SOV_Reperation_Payments = {
	
		icon = paid

		allowed = {
			tag = HUN
		}
		
		available = {
			num_of_civilian_factories_available_for_projects > 4
		}
		visible = {
			has_country_flag = Loan_Repayment_Enabled
			OR = {
				has_idea = soviet_reparations
				has_idea = soviet_reparations2
				has_idea = soviet_reparations3
				has_idea = soviet_reparations4
			}

		}
		
		modifier = {
			civilian_factory_use = 5
		}
		
		ai_will_do = {
			factor = 1
		}
		days_remove = 180
		remove_effect = {
			if = {
				limit = {
					has_idea = soviet_reparations
				}
				remove_ideas = soviet_reparations
				add_ideas = soviet_reparations2
			}
			else_if = {
				limit = {
					has_idea = soviet_reparations2
				}
				remove_ideas = soviet_reparations2
				add_ideas = soviet_reparations3
			}
			else_if = {
				limit = {
					has_idea = soviet_reparations3
				}
				remove_ideas = soviet_reparations3
				add_ideas = soviet_reparations4
			}
			else = {
				remove_ideas = soviet_reparations4
			}
		}
		
	}
	
	bond_25 = {
		icon = togglemoney
		cost = 0
		allowed = {
		}
		available = {
		}
		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
		}
		complete_effect = {
			clr_country_flag = bonds.25
			set_country_flag = bonds.100
		}
	}
	bond_100 = {
		icon = togglemoney
		cost = 0
		allowed = {
		}
		available = {
		}
		visible = {
			has_country_flag = bonds.100
		}
		complete_effect = {
			clr_country_flag = bonds.100
			set_country_flag = bonds.250
		}
	}
	bond_250 = {
		icon = togglemoney
		cost = 0
		allowed = {
		}
		available = {
		}
		visible = {
			has_country_flag = bonds.250
		}
		complete_effect = {
			clr_country_flag = bonds.250
			set_country_flag = bonds.500
		}
	}
	bond_500 = {
		icon = togglemoney
		cost = 0
		allowed = {
		}
		available = {
		}
		visible = {
			has_country_flag = bonds.500
		}
		complete_effect = {
			clr_country_flag = bonds.500
			set_country_flag = bonds.25
		}
	}

####Pan European Union Membership Application###

	PAEU.1 = {
		
		visible = {
			capital_scope = { is_on_continent = europe }

			NOT = {
				has_global_flag = European_Waiting
			}
			NOT = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
			SOV = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
		}

		cost = 150

		complete_effect = {
			every_country = {
				limit = {
					has_dynamic_modifier = {
						modifier = federation_european_union
					}
				}
				country_event = SOV_Beria_Struggle.37
			}
			set_global_flag = { flag = European_Waiting value = 1 days = 30 }
			set_country_flag = PAEU_2_Follow_Up

			custom_effect_tooltip = EU_Waiting_TT
		}
	}
	PAEU.2 = {
		
		visible = {
			capital_scope = { is_on_continent = europe }
			NOT = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}
			SOV = {
				has_dynamic_modifier = {
					modifier = federation_european_union
				}
			}

			has_country_flag = PAEU_2_Follow_Up

			NOT = {
				has_global_flag = European_Waiting
			}
			check_variable = {
				global.federation_european_union_entrance > 1
			}
		}

		cost = 150

		complete_effect = {
			every_country = {
				limit = {
					has_dynamic_modifier = {
						modifier = federation_european_union
					}
				}
				country_event = SOV_Beria_Struggle.35
			}
			add_dynamic_modifier = {
				modifier = federation_european_union
			}

			set_variable = { global.federation_european_union_entrance = 0 }
		}
	}
	PAEU.3 = {

		visible = {
			has_dynamic_modifier = {
				modifier = federation_european_union
			}
		}

		cost = 300

		complete_effect = {
			remove_dynamic_modifier = {
				modifier = federation_european_union 
			}
		}
	}
	
##### DOMESTIC #####
	bond_25_self = {
		icon = bonds

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			check_variable = { outcrowding_limit > 35 }
		}
		
		available = {
			check_variable = { available_liquidity > 25 }
		}
		
		complete_effect = {
			subtract_from_variable = { available_liquidity = 25 }

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Domestic_Interest_Rate
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 84
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_self = {
		icon = bonds

		visible = {
			has_country_flag = bonds.100
			check_variable = { outcrowding_limit > 150 }
		}
		
		available = {
			check_variable = { available_liquidity > 100 }
		}
		
		complete_effect = {
			subtract_from_variable = { available_liquidity = 100 }

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Domestic_Interest_Rate
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 84
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}

	###UK Premium Bonds ##
	bond_premium = {
		icon = bonds

		visible = {
			has_country_flag = Premium_Bonds
		}
		
		available = {
			check_variable = { available_liquidity > 100 }
		}

		days_re_enable = 365
		
		complete_effect = {

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = 1.01
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 84
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_self = {
		icon = bonds

		visible = {
			has_country_flag = bonds.250
			check_variable = { outcrowding_limit > 375 }
		}
		
		available = {
			check_variable = { available_liquidity > 250 }
		}
		
		complete_effect = {
			subtract_from_variable = { available_liquidity = 250 }

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Domestic_Interest_Rate
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 84
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_self = {
		icon = bonds

		visible = {
			has_country_flag = bonds.500
			check_variable = { outcrowding_limit > 750 }
		}
		
		available = {
			check_variable = { available_liquidity > 500 }
		}
		
		complete_effect = {
			subtract_from_variable = { available_liquidity = 500 }

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Domestic_Interest_Rate
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 84
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### IMF #####
	bond_25_GER = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			is_subject = no
			NOT = {
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = GER }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = GER.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = GER.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_GER = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			is_subject = no
			NOT = {
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = GER }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = GER.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = GER.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_GER = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			is_subject = no
			NOT = {
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = GER }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = GER.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = GER.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_GER = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			is_subject = no
			NOT = {
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = GER }
					}
					add_to_variable = { influence_array_val^v = 120 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = GER.id }
				add_to_array = { influence_array_val = 120 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = GER.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### USA #####
	bond_25_USA = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = USA
			NOT = { original_tag = USA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			USA = {
				check_variable = { outcrowding_limit > 35 }
			}
		}
		
		available = {
			USA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = USA }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = USA.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = USA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_USA = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = USA
			NOT = { original_tag = USA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			USA = {
				check_variable = { outcrowding_limit > 150 }
			}
		}
		
		available = {
			USA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = USA }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = USA.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = USA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_USA = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = USA
			NOT = { original_tag = USA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			USA = {
				check_variable = { outcrowding_limit > 375 }
			}
		}
		
		available = {
			USA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = USA }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = USA.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = USA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_USA = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = USA
			NOT = { original_tag = USA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			USA = {
				check_variable = { outcrowding_limit > 750 }
			}
		}
		
		available = {
			USA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = USA }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = USA.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = USA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### FRA #####
	bond_25_FRA = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = FRA
			NOT = { original_tag = FRA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			FRA = {
				check_variable = { outcrowding_limit > 35 }
			}
		}
		
		available = {
			FRA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = FRA }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = FRA.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = FRA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 48
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_FRA = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = FRA
			NOT = { original_tag = FRA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			FRA = {
				check_variable = { outcrowding_limit > 150 }
			}
		}
		
		available = {
			FRA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = FRA }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = FRA.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = FRA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 48
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_FRA = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = FRA
			NOT = { original_tag = FRA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			FRA = {
				check_variable = { outcrowding_limit > 375 }
			}
		}
		
		available = {
			FRA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = FRA }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = FRA.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = FRA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 48
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_FRA = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = FRA
			NOT = { original_tag = FRA }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			FRA = {
				check_variable = { outcrowding_limit > 750 }
			}
		}
		
		available = {
			FRA = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = FRA }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = FRA.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = FRA.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 48
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### ENG #####
	bond_25_ENG = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = ENG
			NOT = { original_tag = ENG }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			ENG = {
				check_variable = { outcrowding_limit > 35 }
			}
		}
		
		available = {
			ENG = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = ENG }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = ENG.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = 1.025
			}
			add_to_array = {
				array = debtOwnerArray
				value = ENG.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_ENG = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = ENG
			NOT = { original_tag = ENG }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			ENG = {
				check_variable = { outcrowding_limit > 150 }
			}
		}
		
		available = {
			ENG = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = ENG }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = ENG.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = 1.025
			}
			add_to_array = {
				array = debtOwnerArray
				value = ENG.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_ENG = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = ENG
			NOT = { original_tag = ENG }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			ENG = {
				check_variable = { outcrowding_limit > 375 }
			}
		}
		
		available = {
			ENG = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = ENG }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = ENG.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = 1.025
			}
			add_to_array = {
				array = debtOwnerArray
				value = ENG.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_ENG = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = ENG
			NOT = { original_tag = ENG }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			ENG = {
				check_variable = { outcrowding_limit > 750 }
			}
		}
		
		available = {
			ENG = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = ENG }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = ENG.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = 1.025
			}
			add_to_array = {
				array = debtOwnerArray
				value = ENG.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 72
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### WGR #####
	bond_25_WGR = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = WGR
			NOT = { original_tag = WGR }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = Salafism
				has_government = radical_shiaism
			}
			WGR = {
				check_variable = { outcrowding_limit > 35 }
			}
		}
		
		available = {
			WGR = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = WGR }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = WGR.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = WGR.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_WGR = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = WGR
			NOT = { original_tag = WGR }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = Salafism
				has_government = radical_shiaism
			}
			WGR = {
				check_variable = { outcrowding_limit > 150 }
			}
		}
		
		available = {
			WGR = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = WGR }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = WGR.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = WGR.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_WGR = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = WGR
			NOT = { original_tag = WGR }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = Salafism
				has_government = radical_shiaism
			}
			WGR = {
				check_variable = { outcrowding_limit > 375 }
			}
		}
		
		available = {
			WGR = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = WGR }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = WGR.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = WGR.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_WGR = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = WGR
			NOT = { original_tag = WGR }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = Salafism
				has_government = radical_shiaism
			}
			WGR = {
				check_variable = { outcrowding_limit > 750 }
			}
		}
		
		available = {
			WGR = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = WGR }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = WGR.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = WGR.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### SOV #####
	bond_25_SOV = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = SOV
			NOT = { original_tag = SOV }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
			SOV = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = SOV }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = SOV.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = SOV.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_SOV = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = SOV
			NOT = { original_tag = SOV }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
			SOV = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = SOV }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = SOV.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = SOV.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_SOV = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = SOV
			NOT = { original_tag = SOV }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
		}
		
		available = {
			SOV = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = SOV }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = SOV.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = SOV.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_SOV = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = SOV
			NOT = { original_tag = SOV }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
			SOV = {
				check_variable = { outcrowding_limit > 1000 }
			}
		}
		
		available = {
			SOV = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = SOV }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = SOV.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = SOV.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### PRC #####
	bond_25_PRC = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				PRC = {
					controls_state = 608
				}
				PRC = {
					controls_state = 592
				}
			}
			date > 1960.1.1
		}
		
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = PRC.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = PRC.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_PRC = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				PRC = {
					controls_state = 608
				}
				PRC = {
					controls_state = 592
				}
			}
			date > 1960.1.1
		}
		
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = PRC.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = PRC.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_PRC = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				PRC = {
					controls_state = 608
				}
				PRC = {
					controls_state = 592
				}
			}
			date > 1970.1.1
		}
		
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = PRC.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = PRC.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_PRC = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			NOT = { 
				has_government = fascism
				has_government = nationalist
				has_government = neutrality
				has_government = Salafism
				has_government = radical_shiaism
			}
			AND = {
				PRC = {
					controls_state = 608
				}
				PRC = {
					controls_state = 592
				}
			}
			date > 1980.1.1
			PRC = {
				check_variable = { outcrowding_limit > 1000 }
			}
		}
		
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = PRC.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = PRC.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	
##### CHI #####
	bond_25_CHI = {
		icon = loans

		visible = {
			NOT = {
				has_country_flag = bonds.100
				has_country_flag = bonds.250
				has_country_flag = bonds.500
			}
			country_exists = CHI
			NOT = { original_tag = CHI }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				CHI = {
					controls_state = 608
				}
				CHI = {
					controls_state = 592
				}
			}
			date > 1960.1.1
			CHI = {
				check_variable = { outcrowding_limit > 35 }
			}
		}
		
		available = {
			CHI = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = CHI }
					}
					add_to_variable = { influence_array_val^v = 5 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = CHI.id }
				add_to_array = { influence_array_val = 5 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 25
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = CHI.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_100_CHI = {
		icon = loans

		visible = {
			has_country_flag = bonds.100
			country_exists = CHI
			NOT = { original_tag = CHI }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				CHI = {
					controls_state = 608
				}
				CHI = {
					controls_state = 592
				}
			}
			date > 1960.1.1
			CHI = {
				check_variable = { outcrowding_limit > 150 }
			}
		}
		
		available = {
			CHI = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = CHI }
					}
					add_to_variable = { influence_array_val^v = 15 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = CHI.id }
				add_to_array = { influence_array_val = 15 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 100
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = CHI.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_250_CHI = {
		icon = loans

		visible = {
			has_country_flag = bonds.250
			country_exists = CHI
			NOT = { original_tag = CHI }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			OR = {
				CHI = {
					controls_state = 608
				}
				CHI = {
					controls_state = 592
				}
			}
			date > 1970.1.1
			CHI = {
				check_variable = { outcrowding_limit > 375 }
			}
		}
		
		available = {
			CHI = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = CHI }
					}
					add_to_variable = { influence_array_val^v = 30 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = CHI.id }
				add_to_array = { influence_array_val = 30 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 250
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = CHI.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	bond_500_CHI = {
		icon = loans

		visible = {
			has_country_flag = bonds.500
			country_exists = CHI
			NOT = { original_tag = CHI }
			is_subject = no
			NOT = { 
				has_government = communism
				has_government = maoism
				has_government = Salafism
				has_government = radical_shiaism
			}
			AND = {
				CHI = {
					controls_state = 608
				}
				CHI = {
					controls_state = 592
				}
			}
			date > 1980.1.1
			CHI = {
				check_variable = { outcrowding_limit > 750 }
			}
		}
		
		available = {
			CHI = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}
		
		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = CHI }
					}
					add_to_variable = { influence_array_val^v = 60 }
					set_country_flag = found
				}
			}
			if = {
				limit = { 
					NOT = { 
						has_country_flag = found 
					} 
				}
				add_to_array = { influence_array = CHI.id }
				add_to_array = { influence_array_val = 60 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			set_variable = {
				pulse_deficit = 500
			}
			add_to_variable = { 
				currentMoney = pulse_deficit 
			}
			multiply_variable = {	#Interest
				pulse_deficit = Favorable_Interest_Rate
			}
			add_to_array = {
				array = debtOwnerArray
				value = CHI.id
			}
			add_to_array = {
				array = debtTotalsArray
				value = pulse_deficit
			}
			add_to_variable = { 
				currentDebt = pulse_deficit
			}
			divide_variable = {
				pulse_deficit = 60
			}
			add_to_variable = { 
				debtPayment = pulse_deficit
			}
			add_to_array = {
				array = debtPaymentArray
				value = debtPayment
			}
			add_to_variable = { currentDebtPayments = debtPayment }
		}
	}
	
	#The TAZARA Railway (PRC Decision)
	
 	Request_Chinese_Technical_Advisors = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			check_variable = { grossdomesticproduct > 35 }
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_technical_advisors
				days = 3650
			}
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 100 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
					add_to_array = { influence_array = PRC.id }
					add_to_array = { influence_array_val = 100 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

		}
	}


 	Request_Chinese_Educational_Support = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_idea = policy_education_temp_0
				has_idea = policy_education_temp_1
			}
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_Educational_Advisors
				days = 3650
			}
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 100 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
					add_to_array = { influence_array = PRC.id }
					add_to_array = { influence_array_val = 100 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

		}
	}


 	Request_Chinese_Medical_Assistance = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_idea = policy_healthcare_temp_0
				has_idea = policy_healthcare_temp_1
			}
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = PRC_Health_Advisors
				days = 3650
			}
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 100 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
					add_to_array = { influence_array = PRC.id }
					add_to_array = { influence_array_val = 100 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

		}
	}



 	Request_Chinese_Military_Advisors = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			has_country_flag = prc_aid_candidate
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 3650


		cost = 0

		complete_effect = {
			add_timed_idea = {
				idea = chinese_advisors
				days = 3650
			}
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 100 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
					add_to_array = { influence_array = PRC.id }
					add_to_array = { influence_array_val = 100 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

		}
	}

	Seek_Chinese_Financial_Aid = {

		icon = generic_industry

		visible = {
			is_subject = no
			country_exists = PRC
			NOT = { original_tag = PRC }
			is_subject = no
			OR = {
				has_government = socialist
				has_government = maoism
				has_government = communism
				has_government = nationalist
				has_government = trotskyism 
				has_government = fascism
			}
		}
		available = {
			PRC = {
				has_opinion = {
					target = ROOT
					value > 25
				}
			}
		}

		fire_only_once = no
		days_re_enable = 1095


		cost = 0

		complete_effect = {
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = PRC }
					}
					add_to_variable = { influence_array_val^v = 25 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
					add_to_array = { influence_array = PRC.id }
					add_to_array = { influence_array_val = 25 }
			}
			clr_country_flag = found
			recalculate_influence = yes	

			add_to_variable = { 
				currentMoney = 25 
			}
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	Pay_Off_Debt = {

		icon = paid

		available = {
			check_variable = { currentMoney > currentDebt }
			check_variable = { currentDebt > 0 }
			NOT = {
				has_country_leader = {
					name = "Lazar Kaganovich"
					ruling_only = yes
				}
			}
		}

		fire_only_once = no


		cost = 0

		complete_effect = {
			subtract_from_variable = { currentMoney = currentDebt }
			for_each_loop = {
				array = debtPaymentArray
				index = i
				
				set_variable = { debtTotalsArray^i = 0 } 
				set_variable = { debtPaymentArray^i = 0 } 	
				set_variable = { debtOwnerArray^i = 0 }
			}
			set_variable = { currentDebt = 0 }
			set_variable = { debtPayment = 0 }
			for_loop_effect = {
				end = influence_array^num
				value = v
				if = {
					limit = {
						check_variable = { influence_array^v = GER }
					}
					set_variable = { influence_array_val^v = 0 }
				}
			}
			recalculate_influence = yes
		}
		
		ai_will_do = {
			factor = 0
		}
	}
	
	
	
}