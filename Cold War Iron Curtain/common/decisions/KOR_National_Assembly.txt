NATIONAL_ASSEMBLY_KOR = {
	deal_effort = {
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
		}
		visible = {
		}
		cost = 50
		
		complete_effect = {
			add_to_variable = {
                naseat = 2
                tooltip = KOR_naseat_pos_tt
            }
		}
		days_remove = 30
		remove_effect = {
			subtract_from_variable = {
				naseat = 2
				tooltip = KOR_naseat_neg_tt
			}
		}
	}
	the_farmland_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_farmland_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_farmland_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_farmland_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_military_service_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_military_service_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			custom_effect_tooltip = KOR_improve_force_situation_TT
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_military_service_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_military_service_act
					add_to_variable = { KOR_M_training = -0.05 }
					add_to_variable = { KOR_M_training_m_level = 0.05 }
					add_to_variable = { KOR_M_training_m = 0.05 }
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	labor_standards_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = labor_standards_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			custom_effect_tooltip = KOR_improve_force_situation_TT
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_labor_standards_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_labor_standards_act
					add_to_variable = { KOR_M_training = -0.05 }
					add_to_variable = { KOR_M_training_m_level = 0.05 }
					add_to_variable = { KOR_M_training_m = 0.05 }
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	revised_national_security_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = revised_national_security_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_revised_national_security_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	nacf_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = nacf_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_nacf_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_nacf_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	nacf_bank_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = nacf_bank_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_nacf_bank_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_nacf_bank_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	korea_coal_corporation_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = {
				naseat > 100
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = korea_coal_corporation_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_korea_coal_corporation_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_korea_coal_corporation_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	bank_of_korea_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = bank_of_korea_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_bank_of_korea_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_bank_of_korea_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	korean_minting_and_security_planning_corporations_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = korean_minting_and_security_planning_corporations_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_korean_minting_and_security_planning_corporations_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_korean_minting_and_security_planning_corporations_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_nationwide_reconstruction_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_nationwide_reconstruction_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_nationwide_reconstruction_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_nationwide_reconstruction_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_embrace_specialist_and_highly_trained_personnel_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_embrace_specialist_and_highly_trained_personnel_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_embrace_specialist_and_highly_trained_personnel_act
				add_research_slot = 1
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_embrace_specialist_and_highly_trained_personnel_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
					add_research_slot = 1
				}
			}
		}
	}
	the_north_region_development_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_north_region_development_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_north_region_development_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_north_region_development_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_anticommunism_education_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_anticommunism_education_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_anticommunism_education_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_the_anticommunism_education_act
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	the_cultural_interactions_and_development_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_cultural_interactions_and_development_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_cultural_interactions_and_development_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_cultural_interactions_and_development_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	local_autonomy_implementation_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = local_autonomy_implementation_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_local_autonomy_implementation_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_local_autonomy_implementation_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	judical_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = judical_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_judical_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_judical_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	reorganized_electoral_system_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = reorganized_electoral_system_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_reorganized_electoral_system_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_reorganized_electoral_system_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	national_unity_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = national_unity_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_national_unity_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_national_unity_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_communism_condemnation_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_communism_condemnation_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_communism_condemnation_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_communism_condemnation_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_public_housing_initiative_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_public_housing_initiative_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_public_housing_initiative_act
				1358 = {
					add_extra_state_shared_building_slots = 2
					add_building_construction = {
						type = industrial_complex
						level = 2
						instant_build = yes
					}
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_public_housing_initiative_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
					1358 = {
						add_extra_state_shared_building_slots = 2
						add_building_construction = {
							type = industrial_complex
							level = 2
							instant_build = yes
						}
					}
				}
			}
		}
	}
	the_pluralism_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_pluralism_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_pluralism_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_pluralism_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_educational_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_educational_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_educational_reform_act
				add_research_slot = 1
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_educational_reform_act
					add_research_slot = 1
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_politcal_gangsters_removal_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_politcal_gangsters_removal_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_politcal_gangsters_removal_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
					if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_politcal_gangsters_removal_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_bureaucratic_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_bureaucratic_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_bureaucratic_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
					if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_bureaucratic_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_budgetary_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_budgetary_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_budgetary_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_budgetary_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_antistriker_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_antistriker_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_antistriker_act
				add_stability = -0.1
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_antistriker_act
					add_stability = -0.1
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_decisive_press_censorship_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_decisive_press_censorship_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_decisive_press_censorship_act
				add_stability = -0.05
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_decisive_press_censorship_act
					add_stability = -0.05
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_primary_industry_aid_and_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_primary_industry_aid_and_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_primary_industry_aid_and_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_primary_industry_aid_and_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	police_modernization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = police_modernization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_police_modernization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_police_modernization_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	antinorth_korean_supporters_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = antinorth_korean_supporters_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_antinorth_korean_supporters_act
				add_opinion_modifier = {
					target = SOV
					modifier = large_decrease
				}
				SOV = {
					add_opinion_modifier = {
					target = KOR
					modifier = large_decrease
					}
				}
				add_opinion_modifier = {
					target = PRC
					modifier = large_decrease
				}
				PRC = {
					add_opinion_modifier = {
					target = KOR
					modifier = large_decrease
					}
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_antinorth_korean_supporters_act
					add_opinion_modifier = {
						target = SOV
						modifier = large_decrease
					}
					SOV = {
						add_opinion_modifier = {
						target = KOR
						modifier = large_decrease
						}
					}
					add_opinion_modifier = {
						target = PRC
						modifier = large_decrease
					}
					PRC = {
						add_opinion_modifier = {
						target = KOR
						modifier = large_decrease
						}
					}
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	urban_planning_modernazation_and_employment_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = urban_planning_modernazation_and_employment_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_urban_planning_modernazation_and_employment_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_urban_planning_modernazation_and_employment_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_capiltalism_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_capiltalism_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_capiltalism_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_capiltalism_reform_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	mass_privatization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = mass_privatization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_mass_privatization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_mass_privatization_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	freedom_of_speech_and_action_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = freedom_of_speech_and_action_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 5
                tooltip = KOR_naseat_pos_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_freedom_of_speech_and_action_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = { 
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_freedom_of_speech_and_action_act
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	national_security_act_amendment = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 150 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = national_security_act_amendment_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = -30
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = -300
				swap_ideas = {
					remove_idea = KOR_national_security_act
					add_ideas = KOR_national_security_act_amendment
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 150 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = -300
					swap_ideas = {
						remove_idea = KOR_national_security_act
						add_ideas = KOR_national_security_act_amendment
					}
					
					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	retail_trade_promotion_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = retail_trade_promotion_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_retail_trade_promotion_act
				750 = {
					add_extra_state_shared_building_slots = 2
					add_building_construction = {
						type = industrial_complex
						level = 2
						instant_build = yes
					}
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_retail_trade_promotion_act
					750 = {
						add_extra_state_shared_building_slots = 2
						add_building_construction = {
							type = industrial_complex
							level = 2
							instant_build = yes
						}
					}

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_freedom_of_press_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_freedom_of_press_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_freedom_of_press_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_freedom_of_press_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	national_assembly_rights_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = national_assembly_rights_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_national_assembly_rights_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_national_assembly_rights_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	total_cabinet_organization_reorganization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = total_cabinet_organization_reorganization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_total_cabinet_organization_reorganization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_total_cabinet_organization_reorganization_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	limited_judical_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = limited_judical_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_limited_judical_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_limited_judical_reform_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	national_security_act_revision_of_1957 = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = national_security_act_revision_of_1957_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				swap_ideas = {
					remove_idea = KOR_national_security_act
					add_ideas = KOR_national_security_act_revision_of_1957
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
					if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					swap_ideas = {
						remove_idea = KOR_national_security_act
						add_ideas = KOR_national_security_act_revision_of_1957
					}

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	major_urban_fortification_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = major_urban_fortification_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_major_urban_fortification_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_major_urban_fortification_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	press_power_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = press_power_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_press_power_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_press_power_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	foreign_investments_encouragement_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = foreign_investments_encouragement_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_foreign_investments_encouragement_act
				1358 = {
					add_extra_state_shared_building_slots = 4
					add_building_construction = {
						type = industrial_complex
						level = 2
						instant_build = yes
					}
					add_building_construction = {
						type = arms_factory
						level = 2
						instant_build = yes
					}
				}
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_foreign_investments_encouragement_act
					1358 = {
						add_extra_state_shared_building_slots = 4
						add_building_construction = {
							type = industrial_complex
							level = 2
							instant_build = yes
						}
						add_building_construction = {
							type = arms_factory
							level = 2
							instant_build = yes
						}
					}

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	industrial_modernization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = industrial_modernization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_industrial_modernization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_industrial_modernization_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	political_police_sanction_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = political_police_sanction_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_political_police_sanction_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_political_police_sanction_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	nationalist_youth_organization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = nationalist_youth_organization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_nationalist_youth_organization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_nationalist_youth_organization_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	recognition_of_the_provisional_government_of_the_republic_of_korea_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = recognition_of_the_provisional_government_of_the_republic_of_korea_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_recognition_of_the_provisional_government_of_the_republic_of_korea_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_recognition_of_the_provisional_government_of_the_republic_of_korea_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	korean_nationalism_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = korean_nationalism_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_korean_nationalism_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_korean_nationalism_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	religious_harmony_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = religious_harmony_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_religious_harmony_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_religious_harmony_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	whiteshirts_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = whiteshirts_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_whiteshirts_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_whiteshirts_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	antipolitical_gangsters_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = antipolitical_gangsters_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_antipolitical_gangsters_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_antipolitical_gangsters_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	political_integration_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = political_integration_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_political_integration_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_political_integration_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	against_allterror_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = against_allterror_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_against_allterror_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_against_allterror_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	political_integration_act_no2 = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = political_integration_act_no2_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_political_integration_act_no2
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_political_integration_act_no2

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	antijapanese_collaborations_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = antijapanese_collaborations_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_antijapanese_collaborations_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_antijapanese_collaborations_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	religious_syncretism_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = religious_syncretism_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_religious_syncretism_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_religious_syncretism_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	domestic_market_revitalization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = domestic_market_revitalization_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_domestic_market_revitalization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_domestic_market_revitalization_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	state_media_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = state_media_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_state_media_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_state_media_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_national_import_and_export_corporations_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_national_import_and_export_corporations_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_national_import_and_export_corporations_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_national_import_and_export_corporations_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_korean_culture_promotion_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_korean_culture_promotion_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_korean_culture_promotion_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_korean_culture_promotion_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_safe_surveillance_and_education_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 180 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_safe_surveillance_and_education_act_enabled
		}
		cost = 300
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_safe_surveillance_and_education_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 180 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_safe_surveillance_and_education_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_dark_cloud_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 180 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_dark_cloud_act_enabled
		}
		cost = 500
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_dark_cloud_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 180 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_dark_cloud_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_stabilization_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 180 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_stabilization_act_enabled
		}
		cost = 800
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_stabilization_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 180 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_stabilization_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	limited_economic_and_finance_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = limited_economic_and_finance_reform_act_enabled
		}
		cost = 100
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_limited_economic_and_finance_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_limited_economic_and_finance_reform_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	new_bureaucracy_model_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = new_bureaucracy_model_act_enabled
		}
		cost = 100
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_new_bureaucracy_model_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_new_bureaucracy_model_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	new_business_support_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = new_business_support_act_enabled
		}
		cost = 100
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_new_business_support_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_new_business_support_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	the_antitax_evaders_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_antitax_evaders_act_enabled
		}
		cost = 100
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_antitax_evaders_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_the_antitax_evaders_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
	central_development_plan_formations_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			check_variable = { naseat > 100 }
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = central_development_plan_formations_act_enabled
		}
		cost = 100
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			add_to_variable = {
                naseat = 3
                tooltip = KOR_naseat_neg_tt
			}
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_central_development_plan_formations_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				if = {
					limit = {
						check_variable = { naseat > 100 } 
					}
					news_event = { id = kor.na.bill.pass }
					add_political_power = 30
					add_ideas = KOR_central_development_plan_formations_act

					clr_country_flag = law_passing
					clear_support_types = yes
				}
			}
		}
	}
}