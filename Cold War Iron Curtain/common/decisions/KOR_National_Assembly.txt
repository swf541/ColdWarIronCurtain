NATIONAL_ASSEMBLY_KOR = {
	deal_effort = {
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
		}
		visible = {
		}
		cost = 50
		
		complete_effect = {
			naseat = 2
		}
		days_remove = 30
		remove_effect = {
			naseat = 2
		}
	}
	the_farmland_reform_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_farmland_reform_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_farmland_reform_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_the_farmland_reform_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	the_military_service_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = the_military_service_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			custom_effect_tooltip = KOR_improve_force_situation_TT
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_the_military_service_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_the_military_service_act
				add_to_variable = { naseat = 5 }
				add_to_variable = { KOR_M_training = -0.05 }
				add_to_variable = { KOR_M_training_m_level = 0.05 }
				add_to_variable = { KOR_M_training_m = 0.05 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	labor_standards_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = labor_standards_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			custom_effect_tooltip = KOR_improve_force_situation_TT
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_labor_standards_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_labor_standards_act
				add_to_variable = { naseat = 5 }
				add_to_variable = { KOR_M_training = -0.05 }
				add_to_variable = { KOR_M_training_m_level = 0.05 }
				add_to_variable = { KOR_M_training_m = 0.05 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	revised_national_security_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = revised_national_security_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_revised_national_security_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	nacf_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = nacf_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_nacf_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_nacf_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	nacf_bank_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = nacf_bank_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_nacf_bank_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_nacf_bank_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	korea_coal_corporation_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = korea_coal_corporation_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_korea_coal_corporation_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_korea_coal_corporation_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	bank_of_korea_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = bank_of_korea_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_bank_of_korea_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_bank_of_korea_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
	korean_minting_and_security_planning_corporations_act = {
	
		allowed = { tag = KOR }
		ai_will_do = { factor = 10 }
		
		available = {
			NOT = { has_country_flag = law_passing }
			limit = {
				check_variable = { naseat > 100 }
			}
		}
		visible = {
			NOT = { has_country_flag = law_passing }
			has_country_flag = korean_minting_and_security_planning_corporations_act_enabled
		}
		cost = 50
		fire_only_once = yes
		
		complete_effect = {
			set_country_flag = law_passing
			
			effect_tooltip = {
				add_political_power = 30
				add_ideas = KOR_korean_minting_and_security_planning_corporations_act
			}
		}
		days_remove = 50
		remove_effect = {
			hidden_effect = {
				limit = { 
					check_variable = { naseat > 100 } 
				}
				news_event = { id = kor.na.bill.pass }
				add_political_power = 30
				add_ideas = KOR_korean_minting_and_security_planning_corporations_act
				add_to_variable = { naseat = 5 }
				
				clr_country_flag = law_passing
				clear_support_types = yes
			}
		}
	}
}