ECSC = {
	TEST_DECISION = {
		icon = GFX_generic_ECSC
		# allowed = {
		# 	has_idea = European_Development
		# }
		available = {
		}
		visible = {
			has_idea = European_Development
		}
		fire_only_once = yes
		cost = 100
		ai_will_do = { factor = 100 }
	
	}
	Attempt_ECSC_Vote_Again= {

		icon = generic_civil_support

		available = {			
			always = no
		}

		days_mission_timeout = 365

		activation = {
			always = no
		}

		cancel_trigger = {

		}


		visible = {
			tag = FRA
		}

		is_good = no

		complete_effect = {
		}
	}
	ECSC_Vote_yes = {

		icon = generic_civil_support

		available = {			
			has_global_flag = ECSC_VOTE_ONGOING
			NOT = {
				if = {
					limit = {
						tag = FRA
					}
					has_global_flag = FRA_VOTE_YES
				}
				else_if = {
					limit = {
						tag = HOL
					}
					has_global_flag = HOL_VOTE_YES
				}
				else_if = {
					limit = {
						tag = BEL
					}
					has_global_flag = BEL_VOTE_YES
				}
				else_if = {
					limit = {
						tag = LUX
					}
					has_global_flag = LUX_VOTE_YES
				}
				else_if = {
					limit = {
						tag = ITA
					}
					has_global_flag = ITA_VOTE_YES
				}
				else_if = {
					limit = {
						tag = WGR
					}
					has_global_flag = WGR_VOTE_YES
				}
			}
		}
		
		visible = {
			has_global_flag = ECSC_Formed_Global
			OR = {
				tag = HOL
				tag = BEL
				tag = LUX
				tag = ITA
				tag = WGR
				tag = FRA
			}
		}

		is_good = no

		complete_effect = {
			if = {
				limit = {
					tag = FRA
				}
				set_global_flag = FRA_VOTE_YES
				clr_global_flag = FRA_VOTE_NO
			}
			else_if = {
				limit = {
					tag = WGR
				}
				set_global_flag = WGR_VOTE_YES
				clr_global_flag = WGR_VOTE_NO
			}
			else_if = {
				limit = {
					tag = ITA
				}
				clr_global_flag = ITA_VOTE_NO
				set_global_flag = ITA_VOTE_YES
			}
			else_if = {
				limit = {
					tag = HOL
				}
				clr_global_flag = HOL_VOTE_NO
				set_global_flag = HOL_VOTE_YES
			}
			else_if = {
				limit = {
					tag = BEL
				}
				clr_global_flag = BEL_VOTE_NO
				set_global_flag = BEL_VOTE_YES
			}
			else_if = {
				limit = {
					tag = LUX
				}
				clr_global_flag = LUX_VOTE_NO
				set_global_flag = LUX_VOTE_YES
			}
		}
	}
	ECSC_Vote_no = {

		icon = generic_civil_support

		available = {			
			has_global_flag = ECSC_VOTE_ONGOING
			NOT = {
				if = {
					limit = {
						tag = FRA
					}
					has_global_flag = FRA_VOTE_NO
				}
				else_if = {
					limit = {
						tag = HOL
					}
					has_global_flag = HOL_VOTE_NO
				}
				else_if = {
					limit = {
						tag = BEL
					}
					has_global_flag = BEL_VOTE_NO
				}
				else_if = {
					limit = {
						tag = LUX
					}
					has_global_flag = LUX_VOTE_NO
				}
				else_if = {
					limit = {
						tag = ITA
					}
					has_global_flag = ITA_VOTE_NO
				}
				else_if = {
					limit = {
						tag = WGR
					}
					has_global_flag = WGR_VOTE_NO
				}
			}
		}

		visible = {
			has_global_flag = ECSC_Formed_Global
			OR = {
				tag = HOL
				tag = BEL
				tag = LUX
				tag = ITA
				tag = WGR
				tag = FRA
			}
		}

		is_good = no

		complete_effect = {
			if = {
				limit = {
					tag = FRA
				}
				set_global_flag = FRA_VOTE_NO
				clr_global_flag = FRA_VOTE_YES
			}
			else_if = {
				limit = {
					tag = WGR
				}
				set_global_flag = WGR_VOTE_NO
				clr_global_flag = WGR_VOTE_YES
			}
			else_if = {
				limit = {
					tag = ITA
				}
				set_global_flag = ITA_VOTE_NO
				clr_global_flag = ITA_VOTE_YES
			}
			else_if = {
				limit = {
					tag = HOL
				}
				set_global_flag = HOL_VOTE_NO
				clr_global_flag = HOL_VOTE_YES
			}
			else_if = {
				limit = {
					tag = BEL
				}
				set_global_flag = BEL_VOTE_NO
				clr_global_flag = BEL_VOTE_YES
			}
			else_if = {
				limit = {
					tag = LUX
				}
				set_global_flag = LUX_VOTE_NO
				clr_global_flag = LUX_VOTE_YES
			}
		}
	}
	ECSC_improve_miners_condition_in_ruhr = {
		icon = GFX_generic_ECSC
		allowed = {
				tag = WGR
			}
		available = {
			51 = { is_owned_and_controlled_by = WGR }
		}
		visible = {
               has_global_flag = ECSC_formed
			
			}
		fire_only_once = yes
		cost = 100
		days_remove = 120
		ai_will_do = { factor = 100 }
	
		remove_effect = {
				51 = {
					add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}
	}
    
    ECSC_improve_miners_conditions_in_brittany = {
		icon = GFX_generic_ECSC
		allowed = {
				tag = FRA
			}
		available = {
			14 = { is_owned_and_controlled_by = FRA }
		}
		visible = {
               has_global_flag = ECSC_formed			
			}
		fire_only_once = yes
		cost = 100
		days_remove = 120
		ai_will_do = { factor = 100 }

    	
		remove_effect = {
				14 = {
					add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}
	}
	ECSC_Three_Year_Mission_GDP = {

		icon = generic_prepare_civil_war
		allowed = {
			tag = FRA
		}

		available = {
			check_variable = {
				eu_gdp > eu_gdp_goal
			}
			always = no
		}
		activation = {
			always = no
		}
		cost = 0

		fire_only_once = no
		selectable_mission = yes

		days_mission_timeout = 1095

		days_re_enable = 0

		complete_effect = {

		}

		timeout_effect = {
			if = {
				limit = {
					OR = {
						check_variable = {
							eu_gdp > eu_gdp_goal
						}
						check_variable = {
							eu_gdp = eu_gdp_goal
						}
					}
				}
				HOL = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
				LUX = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
				BEL = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
				WGR = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
				FRA = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
				ITA = {
					generic_grant_5_percent_influence_of_EU = yes
					add_timed_idea = {
						idea = ECSC_Economic_Upswing
						days = 1825
					}
				}
			}
			else = {
				FRA = {
					add_political_power = -100
					add_stability = -0.01
				}
				BEL = {
					add_political_power = -100
					add_stability = -0.01
				}
				LUX = {
					add_political_power = -100
					add_stability = -0.01
				}
				HOL = {
					add_political_power = -100
					add_stability = -0.01
				}
				WGR = {
					add_political_power = -100
					add_stability = -0.01
				}
				ITA = {
					add_political_power = -100
					add_stability = -0.01
				}
			}
			random_list = {
				33 = {
					eu_gdp_goal_calc = yes
					activate_mission = ECSC_Three_Year_Mission_GDP
				}
				33 = {
					activate_mission = ECSC_Three_Year_Mission_Border_Excercises
				}
				33 = {
					set_global_flag = ECSC_Political_Coordination_Mission
					activate_mission = ECSC_Thee_Year_Mission_Political_Collaboration
				}
				
			}
		}	

		ai_will_do = {
			factor = 1
		}
	}

#so one related to economy and improving the average gdp of the ECSC(idk if that's possible tho)
#one would be each one of the country sending pp in a pool for them to gain bonus if they reach the thresold (prob will be 2500 pp)
#one would be military exercice aka having them send one or two units in a border state for exercises (if i remember you can tell the ai to go somewhere)

#1 - give a timed bonus in construction to everyone and influence on eachother / if they don't do it in time then political power hit for everyone
#2 - everyone influence on eachother and temporary lower trading price between them(if possible) / if they don't loss of relationship for eachother and pp loss
#3 - experience to everyone and timed military boost + random bonus to one general in each country/ if they don't manage to then xp loss and political power loss and us & soviet influence augment slightly(not too much)


}
