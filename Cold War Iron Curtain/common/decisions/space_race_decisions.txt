########################################
# NEW SPACE DECISIONS
# Designed by Polarace / Coded by Harain
########################################~

space_race_decisions = {
	# Mission 1 - Unmanned Object in Orbit/REQ 1955
	unmanned_object_in_orbit = {
		icon = GFX_earth
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel2
			has_tech = hybrid_propellant
			has_tech = mercury_batteries
			has_tech = directional_antenna
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 135
		visible = {
			NOT = { has_country_flag = finished_unmanned_object_in_orbit }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = {
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 1 - Launch Unmanned Array into Orbit"
			set_country_flag = finished_unmanned_object_in_orbit
			add_to_variable = { num_space_race_decisions = 1 }
			add_to_array = { global.space_race_array_country = ROOT.id }
			add_to_array = { global.space_race_array_num_decisions = 1 }
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_1 } }
				hidden_effect = { news_event = space_race_decision_event.1 }
				set_global_flag = space_mission_1
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1955.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}

	# Mission 2 - Animal in Orbit/REQ 1955
	animal_in_orbit = {
		icon = GFX_earth
		allowed = {}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel2
			has_tech = hybrid_propellant
			has_tech = mercury_batteries
			has_tech = directional_antenna
			num_of_civilian_factories_available_for_projects > 9

		}
		cost = 0
		fire_only_once = yes
		days_remove = 135
		visible = { 
			has_country_flag = finished_unmanned_object_in_orbit
			NOT = { has_country_flag = finished_animal_in_orbit }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 2 - Animal in Orbit (Sputnik 2)"
			set_country_flag = finished_animal_in_orbit
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_2 } }
				hidden_effect = { news_event = space_race_decision_event.2 }
				set_global_flag = space_mission_2
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1955.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}

	# Mission 3 - Man in Orbit/REQ 1960
	man_in_orbit = {
		icon = GFX_earth
		allowed = {}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = common_bulkhead
			has_tech = balloon_tanks
			has_tech = cryogenic_rocket
			has_tech = nickel_batteries
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 180
		visible = {
			has_country_flag = finished_unmanned_object_in_orbit
			NOT = { has_country_flag = finished_man_in_orbit }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 3 - Man in Space"
			set_country_flag = finished_man_in_orbit
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_3 } }
				hidden_effect = { news_event = space_race_decision_event.3 }
				set_global_flag = space_mission_3
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}

	# Mission 4 - Unmanned Geosynchronous Orbit/REQ 1955
	geosynchronous_orbit = {
		icon = GFX_earth
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = common_bulkhead
			has_tech = balloon_tanks
			has_tech = cryogenic_rocket
			has_tech = nickel_batteries
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 135
		visible = {
			# has_country_flag = finished_man_in_orbit
			NOT = { has_country_flag = finished_geosynchronous_orbit }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 4 - Geosynchronous Orbit"
			set_country_flag = finished_geosynchronous_orbit
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_4 } }
				hidden_effect = { news_event = space_race_decision_event.4 }
				set_global_flag = space_mission_4
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}

	# Mission 5 - Orbital Rendez-Vous/REQ 1960
	orbital_rendezvous = {
		icon = GFX_Space_Rockets
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = common_bulkhead
			has_tech = balloon_tanks
			has_tech = cryogenic_rocket
			has_tech = nickel_batteries
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 180
		visible = {
			has_country_flag = finished_man_in_orbit
			has_country_flag = finished_animal_in_orbit
			NOT = { has_country_flag = finished_orbital_rendezvous }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 5 - Orbital Rendezvous"
			set_country_flag = finished_orbital_rendezvous
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_5 } }
				hidden_effect = { news_event = space_race_decision_event.5 }
				set_global_flag = space_mission_5
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 6 - Unmanned Moon Flyby/REQ 1955
	unmanned_flyby = {
		icon = GFX_Satellite
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel2
			has_tech = hybrid_propellant
			has_tech = mercury_batteries
			has_tech = directional_antenna
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 180
		visible = {
			# has_country_flag = finished_man_in_orbit
			NOT = { has_country_flag = finished_unmanned_flyby }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 6 - Unmanned Flyby"
			set_country_flag = finished_unmanned_flyby
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_6 } }
				hidden_effect = { news_event = space_race_decision_event.6 }
				set_global_flag = space_mission_6
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1955.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 7 - Unmanned Moon Orbiter/REQ 1960
	unmanned_moon_orbiter = {
		icon = GFX_Moon
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = common_bulkhead
			has_tech = balloon_tanks
			has_tech = cryogenic_rocket
			has_tech = nickel_batteries
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 180
		visible = {
			has_country_flag = finished_unmanned_flyby
			NOT = { has_country_flag = finished_unmanned_moon_orbiter }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 7 - Unmanned Moon Orbiter"
			set_country_flag = finished_unmanned_moon_orbiter
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_7 } }
				hidden_effect = { news_event = space_race_decision_event.7 }
				set_global_flag = space_mission_7
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 8 - Spacewalk/REQ 1965
	spacewalk = {
		icon = GFX_Space_Rockets
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel3
			has_tech = cryogenic_rocket
			has_tech = hydrogen_cells
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 225
		visible = {
			has_country_flag = finished_man_in_orbit
			NOT = { has_country_flag = finished_spacewalk }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 8 - Spacewalk"
			set_country_flag = finished_spacewalk
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_8 } }
				hidden_effect = { news_event = space_race_decision_event.8 }
				set_global_flag = space_mission_8
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 9 - Unmanned Soft Moon Landing/REQ 1965
	unmanned_soft_moonlanding = {
		icon = GFX_Satellite
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel3
			has_tech = cryogenic_rocket
			has_tech = hydrogen_cells
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 225
		visible = {
			has_country_flag = finished_unmanned_moon_orbiter
			NOT = { has_country_flag = finished_unmanned_soft_moonlanding }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 9 - Unmanned Soft Moon Landing"
			set_country_flag = finished_unmanned_soft_moonlanding
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_9 } }
				hidden_effect = { news_event = space_race_decision_event.9 }
				set_global_flag = space_mission_9
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 10 - Lunar Rover/REQ 1965
	lunar_rover = {
		icon = GFX_Moon
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = pressure_vessel3
			has_tech = cryogenic_rocket
			has_tech = hydrogen_cells
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 225
		visible = {
			has_country_flag = finished_unmanned_soft_moonlanding
			NOT = { has_country_flag = finished_lunar_rover }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 10 - Lunar Rover"
			set_country_flag = finished_lunar_rover
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_10 } }
				hidden_effect = { news_event = space_race_decision_event.10 }
				set_global_flag = space_mission_10
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 11 - Monolithic Space Station/REQ 1970
	monolithic_space_station = {
		icon = GFX_Satellite
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = advanced_plumbing
			has_tech = anti_slosh_baffles
			has_tech = cryogenic_rocket
			has_tech = hydrogen_cells
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 270
		visible = {
			has_country_flag = finished_spacewalk
			NOT = { has_country_flag = finished_monolithic_space_station }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 11 - Monolithic Space Station"
			set_country_flag = finished_monolithic_space_station
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_11 } }
				hidden_effect = { news_event = space_race_decision_event.11 }
				set_global_flag = space_mission_11
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}
	# Mission 12 - Manned Soft Landing & Return/REQ 1970
	manned_moon_landing = {
		icon = GFX_Moon
		allowed = {
		}
		custom_cost_trigger = {
			check_variable = { var = currentMoney value = 100 compare = greater_than_or_equals }
		}
		custom_cost_text = decision_cost_money
		available = {
			NOT = { has_country_flag = mission_click_cooldown }
			has_tech = advanced_plumbing
			has_tech = anti_slosh_baffles
			has_tech = cryogenic_rocket
			has_tech = hydrogen_cells
			has_tech = sstv_transmission
			num_of_civilian_factories_available_for_projects > 9
		}
		cost = 0
		fire_only_once = yes
		days_remove = 360
		visible = {
			has_country_flag = finished_lunar_rover
			has_country_flag = finished_unmanned_soft_moonlanding
			NOT = { has_country_flag = finished_manned_moon_landing }
		}
		modifier = { civilian_factory_use = 10 }
		complete_effect = { 
			set_country_flag = {
				flag = mission_click_cooldown
				days = 30
				value = 1
			}
		}
		remove_effect = {
			log = " [ROOT.GetName] finished mission 12 - Manned Soft Landing & Return"
			set_country_flag = finished_manned_moon_landing
			add_to_variable = { num_space_race_decisions = 1 }
			for_loop_effect = {
				end = global.space_race_array_country^num
				value = v
				if = {
					limit = { check_variable = { global.space_race_array_country^v = ROOT } }
					add_to_variable = { global.space_race_array_num_decisions^v = 1 }
					set_country_flag = found
				}
			}
			if = {
				limit = { NOT = { has_country_flag = found } }
				add_to_array = { global.space_race_array_country = ROOT.id }
				add_to_array = { global.space_race_array_num_decisions = 1 }
			}
			clr_country_flag = found
			space_race_get_top_5 = yes
			monthly_power_projection_gain = yes
			if = { limit = { NOT = { has_global_flag = space_mission_12 } }
				if = {
					limit = {
						original_tag = SOV
					}
					hidden_effect = { news_event = space_race_decision_event.122 }
				}
				else = {
					if = { 
						limit = {
							original_tag = USA
						}
						SOV = {
							set_country_flag = space_mission_12_US_first
						}
					}
					hidden_effect = { news_event = space_race_decision_event.12 }
				}
				set_global_flag = space_mission_12
			}
		}
		ai_will_do = {
			base = 0
			modifier = { add = 10 date > 1960.1.1 }
			modifier = { factor = 10 OR = { Tag = USA Tag = SOV } }
			modifier = { factor = 6 OR = { Tag = ENG Tag = PRC } }
			modifier = { factor = 3 OR = { Tag = FRA Tag = RAJ } }
		}
	}

}
space_missions = {
	SPACE_MISSION_part1 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = root.confirmed_time_part_1
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part1
		}
		allowed = {
			has_country_flag = space_mission_ongoing_part1
		}
		fixed_random_seed = no
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				root.confirmed_risk_2 = {
					activate_mission = SPACE_MISSION_part2
					set_country_flag = space_mission_ongoing_part2
				}
				root.confirmed_risk_1 = {
					clear_ships = yes
				}
			}
			clr_country_flag = space_mission_ongoing_part1
		}
	}
	SPACE_MISSION_part2 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = root.confirmed_time_part_2
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part2
		}
		allowed = {
			has_country_flag = space_mission_ongoing_part2
		}
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				root.confirmed_risk_2 = {
					activate_mission = SPACE_MISSION_part3
					set_country_flag = space_mission_ongoing_part3
				}
				root.confirmed_risk_1 = {
					clear_ships = yes
				}
			}
			clr_country_flag = space_mission_ongoing_part2
		}
	}
	SPACE_MISSION_part3 = {
		priority = 10
		icon = generic_ignite_civil_war
        days_mission_timeout = root.confirmed_time_part_3
		fire_only_once = no
		activation = {
			has_country_flag = space_mission_ongoing_part3
		}
		allowed = {
			has_country_flag = space_mission_ongoing_part3
		}
		available = {
			always = no
		}
		is_good = yes
		timeout_effect = {
			random_list = {
				root.confirmed_risk_2 = {
					add_stability = 0.5
				}
				root.confirmed_risk_1 = {
					add_manpower = 10000
				}
			}
			clear_ships = yes
			clr_country_flag = space_mission_ongoing_part3
		}
	}
}