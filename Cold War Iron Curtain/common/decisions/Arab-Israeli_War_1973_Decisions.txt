Arab_Israeli_War_1973 = {
	
	###Equipment

	SYR_request_soviet_equipment = {
		icon = generic_prepare_civil_war
		available = {
			NOT = { has_war_with = SOV }
			has_war_with = ISR
			has_capitulated = no
		}
		cost = 80
		ai_will_do = { factor = 100 }

		visible = {
			has_completed_focus = SYR_Soviet_Arms_Corridor
			SOV = { exists = yes }
		}
		days_re_enable = 30
		complete_effect = {
			SOV = { add_political_power = 45 }
			add_equipment_to_stockpile = {
				type = 
				amount = 
				producer = SOV
			}
		}
	}

	SYR_request_east_german_equipment = {
		icon = generic_prepare_civil_war
		available = {
			NOT = { has_war_with = DDR }
			has_war_with = ISR
			has_capitulated = no
		}
		cost = 40
		ai_will_do = { factor = 100 }

		visible = {
			has_completed_focus = SYR_Eastern-bloc_war_support
			DDR = { exists = yes }
		}
		days_re_enable = 30
		complete_effect = {
			DDR = { add_political_power = 45 }
			add_equipment_to_stockpile = {
				type = 
				amount = 
				producer = DDR
			}
		}
	}
	
	###OOBs

	SYR_cuban_reinforcements = {

		icon = generic_ignite_civil_war
		available = {
			NOT = { has_war_with = CUB }
			has_war_with = ISR
			has_capitulated = no

			OR = {
				is_subject = no
			}
		}
		visible = {
			has_completed_focus = SYR_Support_from_Non-aligned_Allies
			CUB = { exists = yes }
			ISR = {	owns_state = 759 }
		}
		cost = 20
		ai_will_do = {
			factor = 100
		}
		days_re_enable = 30
		complete_effect = {
			load_oob = SYR_cubans
		}
	}

		SYR_arab_reinforcements = {

		icon = generic_ignite_civil_war
		available = {
			NOT = { has_war_with = SAU }
			NOT = { has_war_with = KUW }
			NOT = { has_war_with = MOR }
			has_war_with = ISR
			has_capitulated = no

			OR = {
				is_subject = no
			}
		}
		visible = {
			has_completed_focus = SYR_Support_from_Arab_States
			SAU = { exists = yes }
			KUW = { exists = yes }
			MOR = { exists = yes }
			ISR = {	owns_state = 759 }
		}
		cost = 60
		ai_will_do = {
			factor = 100
		}
		days_re_enable = 30
		complete_effect = {
			load_oob = SYR_arab_reinforcements
		}
	}

		SYR_iraqi_reinforcements = {

		icon = generic_ignite_civil_war
		available = {
			NOT = { has_war_with = IRQ }
			has_war_with = ISR
			has_capitulated = no

			OR = {
				is_subject = no
			}
		}
		visible = {
			has_completed_focus = SYR_Support_from_Arab_States
			IRQ = { exists = yes }
			ISR = {	owns_state = 759 }
		}
		cost = 60
		ai_will_do = {
			factor = 100
		}
		days_re_enable = 30
		complete_effect = {
			load_oob = SYR_iraqi_reinforcements
		}
	}
}