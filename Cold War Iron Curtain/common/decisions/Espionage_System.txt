variables_calculation_category = {
	Espionage_Operation_Slot_1 = {
		#allowed = {
        #    always = no
		#}
		available = {
			always = no
		}
		fixed_random_seed = no
		selectable_mission = yes

		days_mission_timeout = 1
		fire_only_once = no
		activation = {
			always = no
		}
		timeout_effect = {
            set_variable = {
                history_target = slot_one_target
            }
            set_variable = {
                history_personnel_target = slot_one_personnel_target
            }
            if = {
                limit = {
                    check_variable = {
                        history_personnel_target = 0
                    }
                }
                set_variable = {
                    history_personnel_target = 0.5      #NA Setting
                }
            }
            set_variable = {
                slot_one_conclusion_window_toggle = 1
            }
            add_to_variable = { 
                current_operations = -1
            }
            set_variable = {
                paramilitary_squad_id_status^selected_squad_slot_1 = 1
            }          
            set_temp_variable = {
                current_token = slot_one_mission_token
            }

            remove_from_array = {
                current_operation_list = slot_one_mission_token
            }

            #Actual effects
            operation_closeout_effect = yes
		}

	}
}