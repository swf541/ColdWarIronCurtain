UAR_Lebanon_decisions = {
	Spread_Pan_Arab_Propaganda_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
		}
		
		complete_effect = {
			LEB = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.01
				}
				set_temp_variable = { percent_change = 10 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes				
			}
		}

		cost = 100
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Condemn_Israeli_Influence_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
		}
		
		complete_effect = {
			LEB = {
				add_popularity = {
					ideology = monarchism 
					popularity = -0.02
				}
				add_stability = -0.05
				set_temp_variable = { percent_change = 5 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes					
			}
		}

		cost = 125
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Influence_Officers_Corp_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			has_army_experience > 19
		}
		
		complete_effect = {
			army_experience = -20
			LEB = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.02
				}
				set_temp_variable = { percent_change = 10 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes					
			}
		}

		cost = 0
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Appeal_to_Shia_Populace_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
		}
		
		complete_effect = {
			LEB = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.02
				}
				set_temp_variable = { percent_change = 10 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes					
			}
		}

		cost = 100
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}

	Launch_Coup_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			NOT = { LEB = { has_government = nationalist } }

			custom_trigger_tooltip = {
				tooltip = Launch_Coup_LEB_tt
				NOT = { LEB = { has_country_flag = nasserist_coup } }
				LEB = { check_variable = { Sphere_Leader = ROOT.id } }
				check_variable = { LEB.influence_array_calc^0 > 0.50 }
			}
		}
		
		complete_effect = {
			LEB = {
				country_event = PRO_UAR_COUP.1
				set_country_flag = NASSERIST_COUP
			}
		}

		cost = 0
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Integrate_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			LEB = { has_government = nationalist } 
			has_war = no
			custom_trigger_tooltip = {
				tooltip = Integration_UAR
				LEB = { check_variable = { Sphere_Leader = ROOT.id } }
				check_variable = { LEB.influence_array_calc^0 > 0.74 }
			}
			LEB = {  }
		}
		
		complete_effect = {
			LEB = {
				country_event = UAR_INTEGRATION.1
			}
		}

		cost = 0
		days_re_enable = 1625

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}

	Anti_Imperialist_Propaganda_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			NOT = { LEB = { has_government = nationalist } }
		}
		visible = {
			OR = {
				AND = {
					LEB = { check_variable = { Sphere_Leader = ENG.id } }
					check_variable = { LEB.influence_array_calc^0 > 0.50 }
				}
				AND = {
					LEB = { check_variable = { Sphere_Leader = FRA.id } }
					check_variable = { LEB.influence_array_calc^0 > 0.50 }
				}
				AND = {
					LEB = { check_variable = { Sphere_Leader = SPR.id } }
					check_variable = { LEB.influence_array_calc^0 > 0.50 }
				}
			}
		
		}
		complete_effect = {
			LEB = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.02
				}
				set_temp_variable = { percent_change = 5 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes		
				if = {
					limit = {
						LEB = { check_variable = { Sphere_Leader = FRA.id } }
					}
					set_temp_variable = { percent_change = -5 }
					set_temp_variable = { tag_index = FRA.id }
					set_temp_variable = { influence_target = THIS.id }
					change_influence_percentage = yes
				}
				if = {
					limit = {
						LEB = { check_variable = { Sphere_Leader = SPR.id } }
					}
					set_temp_variable = { percent_change = -5 }
					set_temp_variable = { tag_index = SPR.id }
					set_temp_variable = { influence_target = THIS.id }
					change_influence_percentage = yes
				}
				if = {
					limit = {
						LEB = { check_variable = { Sphere_Leader = ENG.id } }
					}
					set_temp_variable = { percent_change = -5 }
					set_temp_variable = { tag_index = ENG.id }
					set_temp_variable = { influence_target = THIS.id }
					change_influence_percentage = yes
				}
				set_temp_variable = { sph_from = PREV.id }
				set_temp_variable = { sph_amount = 10 }
				sph_grant_influence = yes
			}
		}

		cost = 150
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Arm_Al_Mourabitoun_LEB = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			has_equipment = { infantry_equipment > 499 }
			command_power > 2
		}
		
		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -1000
			}
			add_command_power = -2
			LEB = {
				add_stability = -0.05
				add_popularity = {
					ideology = nationalist 
					popularity = 0.05
				}
				set_temp_variable = { percent_change = 10 }
				set_temp_variable = { tag_index = ROOT.id }
				set_temp_variable = { influence_target = THIS.id }
				change_influence_percentage = yes 	
			}
		}

		cost = 50
		days_re_enable = 1625

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}

}