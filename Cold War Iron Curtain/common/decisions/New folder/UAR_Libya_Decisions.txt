UAR_Libya_decisions = {
	Spread_Pan_Arab_Propaganda_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
		}
		
		complete_effect = {
			LBA = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.05
				}
				set_temp_variable = { sph_from = PREV.id }
				set_temp_variable = { sph_amount = 15 }
				sph_grant_influence = yes				
			}
		}

		cost = 100
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Condemn_Monarchy_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			LBA = { has_government = monarchism }
		}
		
		complete_effect = {
			LBA = {
				add_popularity = {
					ideology = monarchism 
					popularity = -0.02
				}
				add_stability = -0.05
				set_temp_variable = { sph_from = PREV.id }
				set_temp_variable = { sph_amount = 5 }
				sph_grant_influence = yes				
			}
		}

		cost = 125
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Influence_Officers_Corp_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			has_army_experience > 20
		}
		
		complete_effect = {
			army_experience = -20
			LBA = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.02
				}
				set_temp_variable = { sph_from = PREV.id }
				set_temp_variable = { sph_amount = 15 }
				sph_grant_influence = yes				
			}
		}

		cost = 0
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Launch_Coup_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			NOT = { LBA = { has_government = nationalist } }

			custom_trigger_tooltip = {
				tooltip = Launch_Coup_LBA_tt
				NOT = { LBA = { has_country_flag = nasserist_coup } }
				LBA = { check_variable = { Sphere_Leader = ROOT.id } }
				check_variable = { LBA.influence_array_calc^0 > 0.50 }
			}
		}
		
		complete_effect = {
			LBA = {
				country_event = PRO_UAR_COUP.1
				set_country_flag = NASSERIST_COUP
			}
		}

		cost = 0
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}
	Integrate_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			LBA = { 
				has_government = nationalist 
				
			} 
			has_war = no
			custom_trigger_tooltip = {
				tooltip = Integration_UAR
				LBA = { check_variable = { Sphere_Leader = ROOT.id } }
				check_variable = { LBA.influence_array_calc^0 > 0.74 }
			}
		}
		
		complete_effect = {
			LBA = {
				country_event = UAR_INTEGRATION.1
			}
		}

		cost = 0

		fire_only_once = yes
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}

	Anti_Imperialist_Propaganda_LBA = {

		icon = generic_prepare_civil_war

		available = {
			tag = EGY
			NOT = { LBA = { has_government = nationalist } }
		}
		visible = {
			OR = {
				AND = {
					LBA = { check_variable = { Sphere_Leader = ENG.id } }
					check_variable = { LBA.influence_array_calc^0 > 0.50 }
				}
				AND = {
					LBA = { check_variable = { Sphere_Leader = FRA.id } }
					check_variable = { LBA.influence_array_calc^0 > 0.50 }
				}
				AND = {
					LBA = { check_variable = { Sphere_Leader = SPR.id } }
					check_variable = { LBA.influence_array_calc^0 > 0.50 }
				}
			}
		
		}
		complete_effect = {
			LBA = {
				add_popularity = {
					ideology = nationalist 
					popularity = 0.02
				}
				set_temp_variable = { sph_from = PREV.id }
				set_temp_variable = { sph_amount = 10 }
				sph_grant_influence = yes
				if = {
					limit = {
						LBA = { check_variable = { Sphere_Leader = FRA.id } }
					}
					set_temp_variable = { sph_from = FRA.id }
					set_temp_variable = { sph_amount = -5 }
					sph_grant_influence = yes
				}
				if = {
					limit = {
						LBA = { check_variable = { Sphere_Leader = SPR.id } }
					}
					set_temp_variable = { sph_from = SPR.id }
					set_temp_variable = { sph_amount = -5 }
					sph_grant_influence = yes
				}
				if = {
					limit = {
						LBA = { check_variable = { Sphere_Leader = ENG.id } }
					}
					set_temp_variable = { percent_change = -5 }
					set_temp_variable = { tag_index = ENG.id }
					set_temp_variable = { influence_target = THIS.id }
					change_influence_percentage = yes 	
				}		
			}
		}

		cost = 150
		days_re_enable = 365

		fire_only_once = no
		selectable_mission = no

		ai_will_do = {
			factor = 0
		}
	}

}