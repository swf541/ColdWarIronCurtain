

NUN_Leader_Change = {
	NUN_Hoang_Chinh_Leader_Change = {
		visible = {
			has_country_flag = NUN_VIN_trigger
		}
		available = {
			has_completed_focus = NUN_Revolutionary_Bureaucracy
		}

		fire_only_once = yes
		
		complete_effect = {
			hidden_effect = {
				NUN_Vong_A_Sang = {
					remove_country_leader_role = {
						ideology = ho_chi_minh_thought
					}
				}
			}
			NUN_Hoang_Chinh = {
				add_country_leader_role = {
					promote_leader = yes
					country_leader = {
						ideology = ho_chi_minh_thought
					}
				}
			}
		}

		ai_will_do = {
			factor = 100
		}
	}
}

VIN_Developmental_Scheme = {
	VIN_Accept_Foreign_Investments = {
		icon = GFX_dollar3
		fire_only_once = yes
		visible = {
			OR = {
				has_completed_focus = VIN_Request_Economic_Aid_From_America
				has_completed_focus = VIN_Request_Economic_Aid_From_Socialist_Countries
			}
		}
		cost = 100
		complete_effect = {
			1760 = {
				add_building_construction = {
					type = office_park
					level = 1
					instant_build = yes
				}
			}
			add_to_variable = {
				currentMoney = 0.1
			}
			set_temp_variable = {
				x = 100
				tooltip = adjust_money_mil_tt
			}
		}
	}
	VIN_Rebuild_River_Levees = {
		icon = GFX_decision_water_infrastructure
		fire_only_once = yes
		state_target = yes
		target_root_trigger = {
			has_completed_focus = VIN_Rebuild_River_Levees_System
		}
		target_array = VIN.core_states
		target_trigger = {
			FROM = {
				is_owned_by = ROOT
			}
		}
		on_map_mode = map_only
		days_remove = 100
		cost = 0
		custom_cost_trigger = {
			check_variable = {
				currentMoney > 0.1
			}
		}
		custom_cost_text = VIN_has_100_mil
		complete_effect = {
			set_temp_variable = { money_to_gain = -0.1 }
			add_money_with_tooltip_effect = yes
		}
		remove_effect = {
			FROM = {
				add_building_construction = {
					type = water_infrastructure
					level = 1
					instant_build = yes
				}
			}
		}	
	}
	VIN_Deforestation = {
		icon = GFX_decision_generic_electricity
		fire_only_once = yes
		state_target = yes
		target_root_trigger = {
			has_completed_focus = VIN_Promote_Khai_Hoang
		}
		target_array = VIN.core_states
		target_trigger = {
			FROM = {
				is_owned_by = ROOT
			}
		}
		on_map_mode = map_only
		days_remove = 200
		cost = 0
		custom_cost_trigger = {
			check_variable = {
				currentMoney > 0.1
			}
		}
		custom_cost_text = VIN_has_100_mil
		complete_effect = {
			set_temp_variable = { money_to_gain = -0.1 }
			add_money_with_tooltip_effect = yes
		}
		remove_effect = {
			FROM = {
				add_extra_state_shared_building_slots = 1
			}
		}
	}
	VIN_Build_Power_Plant = {
		icon = GFX_decision_generic_electricity
		fire_only_once = yes
		state_target = yes
		target_root_trigger = {
			has_completed_focus = VIN_Electrify_Hong_River_Delta
		}
		target_array = VIN.core_states
		target_trigger = {
			AND = {
				FROM = {
					is_owned_by = ROOT
				}
				FROM = {
					free_building_slots = {
						building = industrial_complex
						size > 1
					}
				}
			}
		}
		on_map_mode = map_only
		days_remove = 200
		cost = 0
		custom_cost_trigger = {
			check_variable = {
				currentMoney > 0.5
			}
		}
		custom_cost_text = VIN_has_500_mil
		complete_effect = {
			set_temp_variable = { money_to_gain = -0.5 }
			add_money_with_tooltip_effect = yes
		}
		remove_effect = {
			FROM = {
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
		}
	}
}
