recruit_informers = { 
	icon = GFX_operations_infiltrate_armed_forces
	map_icon = GFX_operations_boost_resistance_map
	name = "Recruit Informers"
	desc = Recruit_informers_desc
	priority = 4
	
	days = 60
	network_strength = 25
	operatives = 1

	visible = {
		network_national_coverage = {
			target = FROM
			value > 0
		}
	}

	equipment = {
		
	}

	available = {
		NOT = {
			has_operation_token = {
				tag = ROOT 
				token = token_informers
			}
		}
	}
	
	awarded_tokens = {
		token_informers
	}

	risk_chance = 0.2
	outcome_extra_chance = 0.25
	outcome_modifiers = { operation_infiltrate_outcome operation_outcome }
	outcome_execute = {
		ROOT = {
			add_operation_token = {
				tag = FROM
				token = token_informers
			}
			set_country_flag = informers_flag
		}
	}
	outcome_extra_execute = { # SCOPE_OPERATION
		random_list = {
			33 = {
				ROOT = {
					add_operation_token = {
						tag = FROM
						token = token_informers
					}
					army_experience = 10
					add_intel = {
						target = FROM
						army_intel = 10
					}
				}
			}
			67 = {
				ROOT = {
					add_operation_token = {
					tag = FROM
					token = token_informers
				}
					add_to_variable = { IRA = -2 }
				}
			}
		}
	}
	phases = { #infiltration
		infiltration_border = { base = 25 }
		infiltration_submarine = { base = 25 }
		infiltration_paradrop = { 
			base = 25 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0.1 #less likely if we have no transports
			}
		}
		infiltration_diplomatic = {
			base = 25
			modifier = {
				factor = 20
				ROOT = { has_war = no }
				FROM = {
					has_war = no
				}
			}
		}
	}
	phases = { #infiltrate military
		infiltrate_military_bribe = { base = 25 }
		infiltrate_military_seduction = { base = 25 }
		infiltrate_military_turned_pow = {
			base = 25
			modifier = {
				FROM = { has_war = no }
				factor = 0
			}
		}
		infiltrate_military_ideological_supporter = {
			base = 25
			modifier = {
				FROM = { has_government = ROOT }
				factor = 0
			}
		}
	}
	phases = { #exfiltration
		exfiltration_air_pickup = { 
			base = 15 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0.1 #less likely if we have no transports
			}
		}
		exfiltration_border = { base = 30 }
		exfiltration_go_to_ground = { base = 30 }
		exfiltration_submarine_pickup = { base = 25 }
	}
}

instigate_infighting = { 
	icon = GFX_operations_infiltrate_armed_forces
	map_icon = GFX_operations_boost_resistance_map
	name = "Instigate Infighting"
	desc = Instigate_infighting_desc
	priority = 5
	
	days = 60
	network_strength = 60
	operatives = 2

	visible = {
		has_operation_token = {
			tag = FROM
			token = token_informers
		}
	}

	equipment = {
		
	}

	required_tokens = {
		token_informers
	}
	
	awarded_tokens = {
	}

	risk_chance = 0.2
	outcome_extra_chance = 0.25
	outcome_modifiers = { operation_infiltrate_outcome operation_outcome }
	outcome_execute = {
		ROOT = {
			add_days_mission_timeout = {
				mission = Irish_Republican_Army
				days = -30
			}
		}
	}
	outcome_extra_execute = { # SCOPE_OPERATION
		random_list = {
			33 = {
				ROOT = {
					add_days_mission_timeout = {
						mission = Irish_Republican_Army 
						days = 20
					}
					add_to_variable = { IRA = -1 }
					add_timed_idea = {
						idea = IRA_infighting
						days = 120
					}
				}
			}
			33 = {
				ROOT = {
					add_days_mission_timeout = {
						mission = Irish_Republican_Army 
						days = 40
					}
					add_to_variable = { IRA = -2 }
					add_timed_idea = {
						idea = IRA_infighting
						days = 240
					}
				}
			}
			33 = {
				ROOT = {
					add_days_mission_timeout = {
						mission = Irish_Republican_Army 
						days = 60
					}
					add_to_variable = { IRA = -3 }
					add_timed_idea = {
						idea = IRA_infighting
						days = 365
					}
				}
			}
		}
	}
	phases = { #infiltration
		infiltration_border = { base = 25 }
		infiltration_submarine = { base = 25 }
		infiltration_paradrop = { 
			base = 25 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0.1 #less likely if we have no transports
			}
		}
		infiltration_diplomatic = {
			base = 25
			modifier = {
				factor = 20
				ROOT = { has_war = no }
				FROM = {
					has_war = no
				}
			}
		}
	}
	phases = { #infiltrate military
		IRA_infighting_false_flag = { base = 50 }
		IRA_infighting_letter_plant = { base = 50 }
	}
	phases = { #exfiltration
		exfiltration_air_pickup = { 
			base = 15 
			modifier = {
				ROOT = { 
					has_equipment = {
						transport_plane_equipment < 1
					}
				}
				factor = 0.1 #less likely if we have no transports
			}
		}
		exfiltration_border = { base = 30 }
		exfiltration_go_to_ground = { base = 30 }
		exfiltration_submarine_pickup = { base = 25 }
	}
}
