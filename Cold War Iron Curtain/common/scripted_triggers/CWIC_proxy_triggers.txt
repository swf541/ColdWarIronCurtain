# ============================================================
# PROXY WARS — eligibility
# A war is a proxy theatre if the blocs could plausibly meddle in it.
# Bloc leaders fighting each other directly is WW3, a different system.
# Two blocs are hardcoded on purpose: has_volunteers_amount_from wants a real
# tag, and there will never be thirty of them. PRC after the Sino-Soviet split
# is one more branch, not a redesign.
# ============================================================
cwic_proxy_eligible = {
	has_war = yes
	is_subject = no
	NOT = { tag = USA }
	NOT = { tag = SOV }
}
