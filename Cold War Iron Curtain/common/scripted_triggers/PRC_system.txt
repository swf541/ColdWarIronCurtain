allowance_more = {
	set_temp_variable = {#设置gdp临时变量
		PRC_temp_gdp = PRC.grossdomesticproduct
	}
	set_temp_variable = {#设置人口临时变量
		PRC_temp_pop_fake = PRC.max_manpower
	}
	divide_temp_variable = {#GDP除以人口数量
		PRC_temp_gdp = PRC_temp_pop_fake
	}
	multiply_temp_variable = {#乘以上限变量
		PRC_temp_gdp = PRC_allowance_max
	}
	set_variable = {#设置对比数据
		PRC_pop_gdp = PRC_temp_gdp
	}
	check_variable = {
		var = PRCallowance 
		value = PRC_pop_gdp
		compare = greater_than
	}
}
allowance_less = {
	set_temp_variable = {#设置gdp临时变量
		PRC_temp_gdp = PRC.grossdomesticproduct
	}
	set_temp_variable = {#设置人口临时变量
		PRC_temp_pop_fake = PRC.max_manpower
	}
	divide_temp_variable = {#GDP除以人口数量
		PRC_temp_gdp = PRC_temp_pop_fake
	}
	divide_temp_variable = {#除以下限变量
		PRC_temp_gdp = PRC_allowance_min
	}
	set_variable = {#设置对比数据
		PRC_pop_gdp = PRC_temp_gdp
	}
	check_variable = {
		var = PRCallowance 
		value = PRC_pop_gdp
		compare = less_than
	}
}
allowance_normal = {
	set_temp_variable = {#设置gdp临时变量
		PRC_temp_gdp = PRC.grossdomesticproduct
	}
	set_temp_variable = {#设置人口临时变量
		PRC_temp_pop_fake = PRC.max_manpower
	}
	divide_temp_variable = {#GDP除以人口数量
		PRC_temp_gdp = PRC_temp_pop_fake
	}
	divide_temp_variable = {#除以下限变量
		PRC_temp_gdp = PRC_allowance_min
	}
	set_variable = {#设置对比数据
		PRC_pop_gdp = PRC_temp_gdp
	}
	check_variable = {
		var = PRCallowance 
		value = PRC_pop_gdp
		compare = greater_than
	}
	set_variable = {#清零
		PRC_pop_gdp = 0
	}
	set_temp_variable = {#设置gdp临时变量
		PRC_temp_gdp = PRC.grossdomesticproduct
	}
	set_temp_variable = {#设置人口临时变量
		PRC_temp_pop_fake = PRC.max_manpower
	}
	divide_temp_variable = {#GDP除以人口数量
		PRC_temp_gdp = PRC_temp_pop_fake
	}
	multiply_temp_variable = {#乘以上限变量
		PRC_temp_gdp = PRC_allowance_max
	}
	set_variable = {#设置对比数据
		PRC_pop_gdp = PRC_temp_gdp
	}
	check_variable = {
		var = PRCallowance 
		value = PRC_pop_gdp
		compare = less_than
	}
}