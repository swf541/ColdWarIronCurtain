TOR_ranking_scorer = {
	targets = {
		target_array = TOR_member_countries
		score = {
			base = 100
			
			modifier = {
				is_faction_leader = yes
				add = 2000
			}

			modifier = {
				is_major = yes
				add = 500
			} 

			modifier = { 
				has_country_flag = TOR_major_member
				add = 200
			}
			
			modifier = {
				set_temp_variable = { GDP_effect = country_GDP }
				multiply_temp_variable = { GDP_effect = 0.1 }
				add = GDP_effect
			}
		}
	}
}